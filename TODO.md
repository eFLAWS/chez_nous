# ✅ Chez Nous (Homee) — TO DO (Correctifs & Petites UI)

> **Usage :** ce fichier complète `PROJECT.md` (anciennement `CHEZ_NOUS_SUIVI_PROJET.md`, renommé). Celui-là garde la vue d'ensemble architecture/roadmap ; **celui-ci est le carnet du quotidien** — bugs à corriger, petits ajustements d'UI, détails de finition. Une ligne = une chose faisable en une session ou moins.
>
> Ajoute une ligne dès qu'une idée ou un bug te vient, même sans date de traitement prévue. On la déplace vers "Traités" une fois faite.

---

## 🐛 Correctifs à faire (bugs connus)

| # | Description | Où | Priorité | Ajouté le |
| :-: | :--- | :--- | :---: | :--- |
| 1 | Le champ `name` (compte) reste un seul champ concaténé alors que les formulaires saisissent prénom/nom séparément — à séparer proprement dans le modèle si on veut les ré-exploiter distinctement ailleurs | `userService.js`, formulaires signup | 🟡 Moyenne | 31/07/2026 |
| 2 | `useHouseholds()` est appelé indépendamment à 3 endroits (`RequireHousehold`, `HouseholdDashboardPage`, `AppLayout`) — 3 fetches Supabase séparés au lieu d'un cache/contexte partagé. Pas de bug visible pour l'instant, mais à surveiller si des flashs de chargement apparaissent | `useHouseholds.js` et ses 3 appelants | 🟢 Basse | 31/07/2026 |
| 3 | Reconnexion du plan 2D/3D à Supabase (`floorPlanService.js`) : seul le cas nominal a été testé en réel (créer/sauvegarder/recharger). **Pas encore vérifié explicitement** : (a) qu'un LOCATAIRE est bien bloqué en écriture par la RLS s'il tente de sauvegarder, (b) que le message de conflit de version s'affiche bien en cas d'édition concurrente (deux onglets sur le même foyer) | `floorPlanService.js` | 🟡 Moyenne | 01/08/2026 |
| 4 | ~~**Régression connue et acceptée temporairement**~~ **Déprioritisé (03/08/2026, décision explicite de Paul)** : `FloorView3D.jsx`/`pathfinding.js` restent sur l'ancien modèle où la marche de l'avatar ne connaît que le TYPE de dalle (floor = praticable) — l'avatar peut traverser un mur sans ouverture en 3D. Paul a mis la Vue 3D complètement de côté ("elle viendra à la toute fin, je ne pense vraiment pas qu'elle soit importante") — plus bloquant pour rien d'autre, priorité rabaissée en conséquence. Si un jour repris : `findPath` doit devenir arête-aware (bloqué par une arête wall-ext/wall-int entre deux cases voisines, sauf si c'est une arête "opening") | `FloorView3D.jsx`, `utils/pathfinding.js` | 🟢 Basse (déprioritisé, voir note) | 01/08/2026 |
| 5 | Refonte mur-arête : composants React (`LayoutEditor.jsx`, `Plan2DView.jsx`, `WallEdges.jsx`) non testés dans un vrai navigateur/Vite — seule la logique pure (`layoutGeneration.js`) a été vérifiée par test Node réel (pas d'accès réseau dans l'environnement d'assistance pour lancer `npm run dev`). À valider visuellement chez toi avant de considérer ce chantier terminé | `LayoutEditor.jsx`, `Plan2DView.jsx`, `WallEdges.jsx` | 🟡 Moyenne | 01/08/2026 |
| 6 | Refonte mur-arête : si le foyer de test a déjà un plan enregistré avec des portes, elles sont au format `{id, floorId, x, y}` (sans `orientation`) — plus reconnues comme portes valides par le nouveau code (silencieusement ignorées, pas de crash, mais "disparaissent"). Réinitialiser le plan de test (`resetHouseholdLayout`, bouton "🗑️ Réinitialiser le plan") avant de valider | `floorPlanService.js` | 🟡 Moyenne | 01/08/2026 |
| 7 | Intégration Profil/Réglages/Préférences/Calendrier (v0.1.2) : aucun des 4 composants n'a été vérifié dans un vrai navigateur/Vite (pas d'accès réseau dans l'environnement d'assistance) — grille du calendrier (calcul du mois, filtres, sélection de jour), interrupteurs de Préférences (dont l'interaction Vue 3D/Économie d'énergie), recherche filtrante de Réglages/Préférences. À valider visuellement chez toi | `features/account/*`, `HouseholdCalendarPage.jsx` | 🟡 Moyenne | 02/08/2026 |
| 8 | Audit glassmorphisme (halos, `backdrop-filter`, `.card-glow`, voir Traités #8) : rendu visuel non vérifié dans un vrai navigateur — en particulier le positionnement des halos derrière `AppLayout`/les pages compte (`position: relative` + `overflow: hidden` ajoutés, à confirmer qu'aucun contenu n'est rogné par erreur) | `AppLayout.css`, `account-pages.css`, `theme.css` | 🟡 Moyenne | 02/08/2026 |
| 9 | Réécriture complète de `Icons.jsx` en style solide/plein (51 icônes, voir Traités) : rendu visuel non vérifié dans un vrai navigateur — vérifié uniquement par compilation (`esbuild`, syntaxe valide) et diff des noms de fonctions (aucun n'a changé). Les icônes utilisant `fillRule="evenodd"` avec plusieurs sous-tracés (loupe, enveloppe, cadenas, cookie, aide...) sont les plus à risque d'un rendu visuel imparfait — à parcourir un par un dans l'app une fois déployé | `components/ui/Icons.jsx` | 🟡 Moyenne | 02/08/2026 |
| 10 | ~~`Plan2DView.jsx` affiche "Aucune tâche"/`N tâches actives` par pièce, mais `room.activeTaskCount` n'existe dans AUCUNE donnée réelle~~ **Résolu le 03/08/2026** : compteur réel (tâches non terminées, groupées par `room_id`) câblé dans `HouseholdSpatialView.jsx` via `useHouseholdTasks`, passé à `Plan2DView` — voir `PROJECT.md` §3.11/§3.12 | `Plan2DView.jsx`, `HouseholdSpatialView.jsx` | — | 02/08/2026 |
| 11 | ~~`RoomDetailView.jsx` : liste de tâches, jauge de propreté, "Dernier ménage", "Gains potentiels" entièrement en données placeholder locales~~ **Largement résolu le 03/08/2026** : liste réelle (`taskService.js`), case à cocher persistée (complète/rouvre réellement), ajout rapide fonctionnel. **Deux éléments du prototype volontairement RETIRÉS plutôt que faits à moitié** : "Gains potentiels"/gemmes (gamification, chantier 🅳 pas commencé, aucune colonne de points) et "Dernier ménage" précis (remplacé par 2 compteurs réels actifs/terminées — voir #12 ci-dessous pour pourquoi). Filtre "Ménage" retiré aussi (aucun champ ne distingue un "type" de tâche aujourd'hui) | `RoomDetailView.jsx` | — | 03/08/2026 |
| 12 | `tasks` n'a pas de colonnes `completed_at`/`completed_by` (seulement `status`/`created_at`) — impossible de savoir honnêtement QUAND/QUI a coché une tâche en dernier ("Dernier ménage" de `RoomDetailView.jsx`, retiré plutôt que fabriqué, voir #11). Petite migration additive à envisager (2 colonnes nullables, non destructive) si cette info devient utile | `tasks` (schéma Supabase), `RoomDetailView.jsx` | 🟢 Basse | 03/08/2026 |
| 13 | **Régression trouvée en inspectant pour styliser `HouseholdTasksPage.jsx`** : `.card-glow`/`.ambient-glow*`/`.no-scrollbar` sont référencées et utilisées (`AmbientGlow.jsx`, `HouseholdCalendarPage.jsx`) et documentées comme ajoutées à `theme.css` le 02/08/2026 (`PROJECT.md`, journal), mais **aucune des deux n'existe réellement** dans le `theme.css` de ce zip (46 lignes, uniquement des tokens de couleur — pas de `.glass`/`.card-glow`/`.ambient-glow`/config Inter/`plumbob-float`). Les halos d'ambiance et bordures dégradées sont donc actuellement invisibles partout dans l'app (className sans effet, pas de crash). Zip probablement antérieur à cette réécriture ou fichier perdu à l'export — **à vérifier côté dépôt réel de Paul avant de réinventer ces styles de mémoire** (risque de ne pas correspondre exactement à l'original) | `theme.css`, `AmbientGlow.jsx` | 🟡 Moyenne | 03/08/2026 |

## 🎨 UI/UX — petites améliorations

| # | Description | Où | Priorité | Ajouté le |
| :-: | :--- | :--- | :---: | :--- |
| 1 | ~~Lien "Règles du Foyer" (pied de page connexion/inscription) pointe vers `#`~~ **Résolu par suppression (03/08/2026)** : `HouseholdRoot.jsx` (et tout son sous-graphe dormant) supprimé pour de bon, voir `README.md`/`PROJECT.md` §3.6 | ~~`HouseholdRoot.jsx`~~ | — | 31/07/2026 |
| 2 | ~~Lien "Consulter le guide" (tableau de bord des logements) est un placeholder~~ **Résolu par suppression (03/08/2026)** : `HousingDashboard.jsx` supprimé pour de bon | ~~`HousingDashboard.jsx`~~ | — | 31/07/2026 |
| 3 | ~~Sous-titre d'inscription "Créer votre foyer" utilise un infinitif...~~ **Résolu par suppression (03/08/2026)** : `HouseholdRoot.jsx` supprimé pour de bon | ~~`HouseholdRoot.jsx`~~ | — | 31/07/2026 |
| 4 | ~~Bouton "Rejoindre" (modale `JoinHousingModal`) reste désactivé~~ **Résolu par suppression (03/08/2026)** : `JoinHousingModal.jsx` supprimé pour de bon — le vrai système d'invitation par code (chantier 🅵) partira de zéro sur `AppLayout`/Supabase, pas de cette modale | ~~`JoinHousingModal.jsx`~~ | — | 31/07/2026 |
| 5 | La cloche de notifications a été retirée du header (remplacée par le menu profil) — pas de nouvel emplacement prévu pour l'instant ; à réintroduire quelque part si de vraies notifications arrivent un jour (voir `PROJECT.md`, canal de notification encore une question ouverte) | `AppLayout.jsx` | 🟢 Basse | 01/08/2026 |
| 6 | Vérifier visuellement la barre de navigation basse (6 onglets depuis l'ajout de "Dépenses") sur un très petit écran (iPhone SE ou équivalent) — tailles réduites au moment de l'ajout mais jamais vérifiées sur un écran réellement étroit | `AppLayout.css` | 🟢 Basse | 01/08/2026 |
| 7 | Cible tactile des segments de mur (outil de retrait de mur, `LayoutEditor.jsx`) : segment de 16px d'épaisseur, sous la recommandation habituelle de 44×44px pour une zone cliquable au doigt — fonctionnel mais perfectible (zone de tolérance invisible plus large, par ex.) | `LayoutEditor.jsx`, `LayoutEditor.css` | 🟢 Basse | 01/08/2026 |
| 8 | ~~Rendu des portes (`WallEdges.jsx`) : simple trait pointillé de couleur distincte pour l'instant — un symbole d'arc d'ouverture serait plus lisible/professionnel~~ **Résolu différemment (01/08/2026)** : le concept n'est plus une "porte" mais une ouverture (mur retiré) — plus aucun trait dessiné du tout à cet endroit (voir journal `PROJECT.md`), un symbole d'arc n'aurait plus de sens ici puisqu'il n'y a justement plus d'objet à représenter | `WallEdges.jsx` | — | 01/08/2026 |
| 9 | Plusieurs items de `SettingsPage.jsx`/`PreferencesPage.jsx` restent inertes, badge "Bientôt" (Foyers, Confidentialité, Réseaux sociaux, Cookies, Abonnement, Aide & FAQ, Contacter le support, mentions légales, tous les sous-items de "Notifications & Rappels") — aucune page de destination construite. Voir `PROJECT.md` §3.10 pour le détail complet ; à construire au fur et à mesure plutôt qu'un item unique | `features/account/SettingsPage.jsx`, `PreferencesPage.jsx` | 🟢 Basse | 02/08/2026 |
| 10 | `floors[0]` utilisé comme convention de "premier étage" à deux endroits maintenant (`HouseholdSpatialView.jsx` et, depuis le 03/08, `HouseholdHomePage.jsx`) sans tri explicite par `level` — l'ordre dépend de celui renvoyé par Supabase, pas garanti être RDC en premier si plusieurs étages existent. Limite préexistante (pas introduite le 03/08), maintenant dupliquée à un second endroit — un tri centralisé (ex. petite fonction utilitaire partagée) éviterait de la corriger deux fois séparément si elle devient gênante | `HouseholdSpatialView.jsx`, `HouseholdHomePage.jsx` | 🟢 Basse | 03/08/2026 |

**⚠️ Note sur les entrées #1 à #4 ci-dessus (UI/UX) :** ~~elles concernent toutes des fichiers **dormants**... elles redeviendront pertinentes si ce code dormant sert de référence... sinon elles s'évaporeront avec lui à sa suppression~~ **Suppression effective le 03/08/2026** — `HouseholdRoot.jsx`, `HousingDashboard.jsx`, `JoinHousingModal.jsx` (et le reste du sous-graphe mort) ont bien été supprimés pour de bon cette fois, voir `README.md`/`PROJECT.md` §3.6. Les 4 items ci-dessus se sont donc "évaporés avec lui", exactement comme prévu.

## ✅ Traités

| # | Description | Résolu le |
| :-: | :--- | :--- |
| 1 | Après ajout de badges streak/gemme cliquables (`StreakModal`, `/rewards`), deux fichiers modifiés (`AppRouter.jsx`, `AppLayout.css`) avaient été sauvegardés au mauvais endroit (`features/household/AppRouter.jsx` et `src/AppLayout.css` au lieu de `src/AppRouter.jsx` et `src/features/household/AppLayout.css`) — la route `/rewards` n'était jamais réellement montée, les styles de survol jamais chargés. Fusionnés dans les bons fichiers | 31/07/2026 |
| 2 | Une reconnexion ramenait sur la dernière page ouverte avant déconnexion au lieu de l'Accueil (`LoginPage.jsx` utilisait `location.state.from`) — corrigé : connexion → toujours `/households`, redirection auto vers l'unique foyer si un compte n'en a qu'un | 31/07/2026 |
| 3 | ~~Fichiers orphelins sans plus aucun importeur : `HouseholdViewPage.jsx`/`.css`..., `App.jsx`..., `features/household/Icons.jsx`... — supprimés~~ **Correction (03/08/2026) : cette affirmation était fausse pour 2 des 3 fichiers.** `App.jsx` a bien été supprimé comme annoncé. `HouseholdViewPage.jsx`/`.css` et `features/household/Icons.jsx` sont en réalité restés physiquement présents dans le zip à travers plusieurs sessions/révisions ultérieures (constaté à nouveau le 03/08/2026 en réinspectant un zip fraîchement fourni) — **réellement supprimés cette fois**, voir `README.md`/`PROJECT.md` §3.6. Où était le problème : "documenté comme fait" côté Claude ≠ "réellement fait" côté dépôt de Paul, une divergence qui s'est répétée trois fois sur ce projet (`HouseholdViewPage.jsx` deux fois, `features/household/Icons.jsx` une fois) avant d'être enfin vérifiée en pratique plutôt que supposée | 31/07/2026 |
| 4 | Incohérence interne à `DATAMODEL.md` : `password_hash` listé comme colonne obligatoire de `users` dans l'ERD et le dictionnaire, alors que la section 6 du même document affirmait déjà son absence (Supabase Auth gère ça dans `auth.users`) — retiré des deux premiers endroits | 01/08/2026 |
| 5 | Renommage terminologique et de fichiers (demande de Paul) : "vue 2.5D"/"2.5D" → "vue 3D"/"3D" partout ; `ApartmentSpatialMvp.jsx`/`.css` → `HouseholdSpatialView.jsx`/`.css` ; `FloorView2D.jsx`/`.css` → `FloorView3D.jsx`/`.css` ; préfixe CSS `spatial-mvp__` → `spatial-view__`. Appliqué au code (frontend+backend) et à toute la documentation, vérifié à zéro résidu par grep | 01/08/2026 |
| 6 | Calendrier : la sélection d'un jour (carré vert) disparaissait au survol/tap au lieu de s'éclaircir — `.household-calendar-page__day:hover` (une classe + un pseudo-classe) avait la même spécificité CSS que `.household-calendar-page__day--selected` (une classe), et l'emportait par l'ordre des règles. **Le fix du 02/08 (filter: brightness au survol) n'a pas suffi en mobile, toujours signalé cassé par Paul** — remplacé par un anneau `box-shadow` universel (ne partage ni `border` ni `background`, ne peut plus entrer en conflit avec aucun état). Ajouté aussi `appearance: none`/`-webkit-appearance: none` — hypothèse que l'apparence native d'un `<button>` sur certains navigateurs mobiles écrasait le fond même avec `background: none` explicite (`HouseholdCalendarPage.css`). **Pas encore confirmé en navigateur réel** (pas d'accès réseau ici) — hypothèse la plus probable, pas une certitude | 02/08/2026 |
| 7 | Calendrier : nouveau système à 3 états pour les jours (au-delà de sélectionné/aujourd'hui) — bordure fine = jour avec des événements prévus (ni aujourd'hui ni sélectionné), bordure épaisse = aujourd'hui (non sélectionné), carré plein = sélectionné (priorité la plus haute, écrase les deux autres) (`HouseholdCalendarPage.jsx`, `.css`) | 02/08/2026 |
| 8 | Audit complet des effets glassmorphiques suite à une nouvelle directive de conversion enregistrée en mémoire (ne jamais supprimer/simplifier halos, `backdrop-filter`, `.card-glow`) — régression trouvée sur `HouseholdHomePage.jsx` (`.card-glow` jamais implémenté, depuis la toute première page traduite), `HouseholdSwitcher.css`/`UserMenu.css`/`PlaceholderModal.css` (boutons "verre" sans `backdrop-filter`), et l'intégralité des pages compte + Calendrier (aucun halo d'ambiance). Corrigé avec 2 utilitaires globaux réutilisables (`.card-glow`/`.ambient-glow*` dans `theme.css`, composant `AmbientGlow.jsx`) plutôt que dupliqués par page — halos posés une seule fois sur `AppLayout.jsx` (couvre les 6 onglets du foyer d'un coup) + une fois par page compte | 02/08/2026 |
| 9 | **Réorganisation `frontend/src` + suppression massive de code mort** (demande explicite de Paul, confirmée avant exécution) : `features/floor-plan/` créé (bloc spatial extrait de `household/`, ~10 fichiers), `UserMenu.jsx/.css` → `features/account/`, et un sous-graphe mort d'une trentaine de fichiers/dossiers supprimé pour de bon (détail complet dans `README.md`/`PROJECT.md` §3.6) — traqué par chaîne d'import réelle depuis `AppRouter.jsx`/`main.jsx`, pas supposé. `household/` passe de 58 (avant `tasks/`) à 25 fichiers | 03/08/2026 |

---

### Légende priorité
🔴 Haute (bloque un usage courant) · 🟡 Moyenne (gênant, pas bloquant) · 🟢 Basse (finition/cosmétique)
