# 🏠 Chez Nous (Homee) — Suivi de Projet

> **Document de référence vivant.** Ce fichier sert de trame d'avancement : il fige l'état connu du projet à une date donnée, et se complète au fil des sessions avec les idées, correctifs et décisions à venir (section *Backlog* en bas de fichier). Voir aussi **`PRODUCTVISION.md`** (document compagnon : objectifs et fonctionnalités visées, questions ouvertes) — celui-ci répond à "où en est le code", l'autre à "que doit faire l'app".
>
> **Dernière mise à jour du contenu :** 1er août 2026
> **Source :** Documentation projet (`README.md`, `DATAMODEL.md`, `ROUTINGANDUSERFLOWS.md`, `USERFLOWONBOARDING.md`, `PRODUCTVISION.md`, code source backend/frontend)

---

## 1. Qu'est-ce que Chez Nous (Homee) ?

**Chez Nous** (nom commercial : **Homee**) est une application SaaS de gestion de foyer, pensée pour être un produit commercialisable — pas un prototype jetable. Elle cible les **colocations**, **familles** et **couples** qui partagent un même logement.

### Objectif produit
Donner à un foyer un espace commun numérique organisé **autour de l'espace physique réel du logement** : au lieu d'une simple liste de tâches ou d'un tableur de dépenses déconnecté du contexte, chaque tâche, chaque note, chaque objet est rattaché à une pièce précise d'un plan 2D/3D interactif du logement.

### Modules fonctionnels visés

| Module | Description | Statut |
| :--- | :--- | :---: |
| **Plan interactif 2D/3D** | Éditeur de plan (murs, pièces, portes, mobilier), navigable en vue du dessus (2D) ou isométrique (3D) | 🟢 Construit, connecté à Supabase et testé en conditions réelles (§3.9, §5) |
| **Rôles & permissions** | PROPRIETAIRE (édition du plan, gestion des membres) / LOCATAIRE (lecture seule sur le plan, accès complet au reste) | 🟢 Fondations backend + affichage frontend faits |
| **Gestionnaire de tâches** | Tâches ménagères liées à une pièce et/ou un occupant (humain ou animal), récurrence, groupes, assignation multi-personnes, dépendances | 🟡 Schéma V1 exécuté sur Supabase réel (03/08/2026, §3.11) — service/UI frontend pas encore branchés (`HouseholdTasksPage.jsx` toujours placeholder) |
| **Dépenses partagées** | Suivi des dépenses du foyer, réparties entre occupants | 🔴 Spécifié (schéma + RLS), aucun code |
| **Notes spatiales** | Notes rattachées à un point précis du plan | 🔴 Spécifié (mention dans la matrice de droits), aucun code |
| **Mobilier** | Objets positionnés dans les pièces, porteurs de tâches | 🟡 Modèle de tuiles existant, éditeur dédié non actif |
| **Gamification** | Mécaniques ludiques pour motiver l'entretien du foyer | 🔴 Objectif produit, pas encore de traduction technique |
| **Invitation par code** | Rejoindre un foyer existant via un code à 6-8 caractères | 🔴 Placeholder UI uniquement, non fonctionnel |
| **Scan de plan (IA)** | Génération du plan à partir d'une photo | 🔴 Placeholder UI uniquement, non fonctionnel |

Légende : 🟢 Fonctionnel · 🟡 Partiel / en pause · 🔴 Non commencé ou placeholder

### Valeurs UX non négociables
- **Mobile-first** : conçu d'abord pour un écran de smartphone vertical, zones cliquables ≥ 44×44px, mises en page fluides (Flexbox/Grid).
- **Dark mode natif**, esthétique glassmorphique, thème "sombre néon".
- **Palette émeraude/teal** (`#34d399`, `#2dd4bf`) comme identité visuelle.
- **Vraies routes URL** (React Router) partout — jamais de state en mémoire pour simuler de la navigation (impératif pour le geste "retour" mobile et les liens d'invitation directs).

---

## 2. Vue d'ensemble de l'architecture actuelle

**⚠️ Correction (voir la conversation) : le schéma ci-dessous, dans une version antérieure de ce document, affirmait qu'"aucune requête de données ne passe encore par Supabase" — c'est faux, vérifié en lisant le code plutôt qu'en supposant.** La réalité est plus fine, à trois vitesses différentes selon la donnée :

```
┌───────────────────────────────────────────────────────────────┐
│  DÉJÀ SUR SUPABASE (vérifié : householdService.js interroge    │
│  réellement household_members/households, pas l'ancien backend)│
│                                                                 │
│  Auth (session réelle, AuthContext)                            │
│  Foyers / membres / rôles (créer, lister, détail, invite_code) │
│  Plan 2D/3D (floor_plans.layout_data, JSONB, verrou optimiste) │
│  → floorPlanService.js, reconnecté ET testé en conditions      │
│  réelles par Paul (créer/sauvegarder/recharger un plan), §3.9  │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│  UI CONSTRUITE, AUCUNE PERSISTANCE RÉELLE (état local React    │
│  uniquement — se réinitialise au rechargement)                 │
│                                                                 │
│  Accueil (données statiques), Calendrier (grille/filtres/jours │
│  réellement interactifs, événements mock, §3.10), Profil/      │
│  Réglages/Préférences (interrupteurs cliquables mais non       │
│  sauvegardés, §3.10), icône de foyer (aperçu local uniquement) │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│  AUCUNE COUCHE DE DONNÉES NI UI RÉELLE — placeholders          │
│  statiques minimalistes (TabPlaceholder)                        │
│                                                                 │
│  Tâches, Dépenses, dette de ménage, gamification (streak/points)│
└───────────────────────────────────────────────────────────────┘
```

**Correction au passage** (le bloc ci-dessus décrivait encore le plan 2D/3D comme "construit mais sur l'ancien backend, pas monté" — obsolète depuis sa reconnexion Supabase et son test réel par Paul, §3.9, jamais répercuté ici jusqu'à cette relecture) : ce n'est pas "l'auth est migrée, le reste est sur l'ancien backend" comme affirmé précédemment — les foyers/rôles **et le plan 2D/3D** sont déjà pleinement sur Supabase et validés par un test réel. Le vrai manque aujourd'hui, ce sont les données métier du quotidien (tâches, dépenses) : aucune des deux n'a la moindre ligne de service, ni ancien backend ni Supabase.

---

## 3. État détaillé par blocs

### 3.1 — Backend "cœur métier" (Node.js, zéro dépendance)

- Stockage : fichier JSON à plat (`store.json`), lecture/écriture atomique et sérialisée, avec sauvegarde (`store.json.bak`).
- Architecture en services découplés : `userService` (comptes, auth, invitations, occupants), `roomService` (étages/pièces/portes/collisions), `taskService`, `projectService`.
- Toutes les fonctions asynchrones suivent le contrat `{ success, data?, error? }` de bout en bout, jusque dans les composants React.
- **197/197 tests passants** (176 tests historiques + 21 dédiés au système de rôles).
- Script de smoke-test (`live-test.js`) qui démarre un vrai serveur et l'interroge en HTTP réel, en plus des tests unitaires.

**Modèle Occupants vs Comptes** : un occupant (humain ou animal) est une entité distincte d'un compte utilisateur. Un occupant humain peut exister sans compte, puis être "réclamé" (`claimedByUserId`) par un compte réel. Un animal n'a jamais de rôle. Ce découplage permet d'assigner une tâche à quelqu'un qui n'a pas encore de compte, et de superposer l'authentification sans casser le modèle de tâches.

**Rôles PROPRIETAIRE / LOCATAIRE — fondations backend :**
- Rôle assigné à la création : le fondateur d'un foyer (inscription ou création d'un foyer supplémentaire) reçoit automatiquement `PROPRIETAIRE`. Tout occupant humain ajouté ensuite reçoit `LOCATAIRE` par défaut.
- Autorisation vérifiée via `isHouseholdOwner()` avant toute écriture sur le plan (créer/déplacer/supprimer pièce, étage, porte). Les fonctions de lecture restent ouvertes aux deux rôles.
- Ordre de vérification volontaire : la validité de la ressource (le foyer/étage existe-t-il ?) est vérifiée **avant** l'autorisation de rôle, pour des messages d'erreur plus utiles.
- Départ / transfert de propriété : un PROPRIETAIRE seul dans le foyer peut partir librement (déclenche une suppression cascade complète : foyer, plan, tâches, dépenses). Un PROPRIETAIRE avec d'autres occupants ne peut pas partir sans transfert explicite au préalable. Le nombre d'occupants restants est **recalculé côté serveur** à chaque suppression, jamais fait confiance au frontend.

### 3.2 — Éditeur de plan 2D (`LayoutEditor.jsx`, `layout-editor/`) — historique, voir §3.13

⚠️ **`LayoutEditor.jsx` a été entièrement remplacé par `PlanEditorView.jsx` le 03/08/2026** (voir §3.13 pour l'état actuel — refonte UI complète + modèle porte/fenêtre/passage réel). Cette section reste telle quelle pour l'historique des décisions (mur-arête, verrou de pièce d'alors, etc.) — **la mécanique mur-arête et la résolution anti-chevauchement décrites ci-dessous sont toujours d'actualité** (reprises sans changement dans `PlanEditorView.jsx`), mais le **verrou de pièce par cadenas a disparu** (remplacé par le bouton "Ajuster", mode d'édition global — voir §3.13), tout comme la barre d'outils permanente (regroupée en menus).

Le cœur spatial de l'application.

- **Tracé de pièces** : un seul modèle d'interaction (Pointer Events, souris/tactile unifiés) — case de départ, prévisualisation en pointillés, validation au relâchement (refusée sous 2×2 dalles ou en cas de chevauchement). Taille minimale d'une pièce réduite depuis à **1×1 dalle**.
- **Déplacement de pièces** : poignée centrale ou appui long (500ms). ~~Aimantage magnétique pendant le glissé~~ **Correction (01/08/2026, en relisant `roomCollision.js`/`LayoutEditor.jsx` plutôt qu'en supposant) : PAS d'attraction pendant le glissé** — cadrillage pur, la pièce suit le pointeur exactement (l'aimantage avait été retiré à une demande explicite antérieure et n'a jamais été réintroduit, contrairement à ce qu'affirmait une version précédente de cette ligne). Seule la résolution anti-chevauchement (`resolveOverlap`) s'applique, **au relâchement uniquement**, avec correction d'un bug d'oscillation entre pièces voisines (le calcul traite tous les chevauchements simultanés, pas un à la fois).
- **Ouvertures de mur — refonte mur-arête (01/08/2026, voir la conversation)** : ~~génération automatique de porte entre deux pièces séparées d'exactement 1 case~~ retirée. Une ouverture est une **arête** (bordure entre deux cases) marquée comme retirée, pas une case de grille — elle ne peut exister qu'entre deux pièces qui se **touchent réellement** (gap=0), l'inverse de l'ancien modèle. Plus d'auto-détection : toutes les frontières entre pièces sont des murs pleins tant que l'utilisateur n'en retire pas un pan via l'outil (`findOpenableWallSegments`, énumère tous les segments d'arête disponibles le long d'une cloison). **Reformulation (même jour, demande explicite de Paul)** : le concept n'est plus une "porte" ajoutée mais un pan de MUR RETIRÉ — le rendu (`WallEdges.jsx`) ne dessine plus aucun trait stylé "porte", juste une absence totale de trait à cet endroit. Bouton renommé "🧱 Retirer un mur" (était "🚪 Ajouter une porte"). Le champ persisté reste `doors` (aucune migration, changement purement conceptuel/interaction).
- **Modèle de données** : les pièces (`rooms`, simples rectangles `{id, x, y, width, height}`) restent la source de vérité éditable. ~~les dalles de sol/mur/porte sont dérivées automatiquement~~ **Correction (01/08/2026)** : seules les dalles de **sol** sont dérivées (`generateFloorTiles` → `tiles`, un type unique désormais) ; les murs/portes sont dérivés séparément sous forme d'**arêtes** (`computeRoomEdges` → `edges`), jamais de dalles de grille — voir `DATAMODEL.md` §5 pour le détail de l'algorithme et la justification (l'ancien modèle mur-dalle faisait fusionner visuellement deux pièces collées, faute de case disponible pour un mur entre elles). Opération inverse (`extractRoomRectsFromTiles`) pour recharger un plan existant dans l'éditeur — inchangée, toujours basée sur les dalles de sol.
- **Limite assumée** : uniquement des pièces rectangulaires (pas de formes en L). Le mobilier existant est réappliqué à la sauvegarde seulement s'il retombe sur la même pièce qu'avant, sinon silencieusement abandonné.
- **Verrou de pièce (01/08/2026, voir la conversation)** : chaque pièce porte un cadenas (coin supérieur gauche, 🔒/🔓). Verrouillée (défaut, y compris les pièces déjà existantes à l'ouverture) : nom + superficie affichés (`{surface} m² ({largeur}×{hauteur})`), déplacement/redimensionnement désactivés — corrige au passage le nom de pièce jusque-là caché derrière la poignée ✢ (les deux ne s'affichent plus jamais en même temps). Déverrouillée : poignée ✢ + 4 poignées de redimensionnement, bordure émeraude mise en évidence. **Une seule pièce déverrouillée à la fois** (état scalaire unique) : en déverrouiller une reverrouille automatiquement l'ancienne ; se reverrouille aussi automatiquement dès qu'on commence à tracer une nouvelle pièce et à l'enregistrement du plan. Une pièce fraîchement créée démarre déverrouillée. Renommer/changer le type (Inspecteur) reste possible uniquement pièce verrouillée (cible du nom non rendue si déverrouillée) — reverrouiller d'abord si besoin.
- **Création enrichie + couleur indépendante du type (02/08/2026, demande explicite de Paul)** : `RoomNameModal.jsx` → renommé **`RoomCreateModal.jsx`** (demandait seulement le nom, type imposé par défaut) — demande maintenant **type ET couleur** dès la création, en plus du nom. Choisir un type suggère un nom + une couleur par défaut (celle du type), mais tout reste librement modifiable avant validation. Nouvelle palette `ROOM_COLORS` (14 couleurs vives, `roomTypes.js`) — INDÉPENDANTE des couleurs pastel de type : avant, la couleur était TOUJOURS dérivée du type (changer de type l'écrasait) ; ce n'est plus le cas, `RoomInspector.jsx` porte désormais la même grille de couleurs pour changer une pièce déjà créée sans que ça touche à son type/nom/icône.
- **"🗑️ Supprimer cet étage" déplacé dans l'éditeur (02/08/2026, demande explicite de Paul)** : retiré du menu de vue du plan (où vivait encore "+ Étage" avant son propre déplacement), ajouté dans `LayoutEditor.jsx` à côté de "Réinitialiser cet étage" — masqué (`onDeleteFloor` absent) si l'étage en cours n'existe pas encore côté serveur (rien à supprimer sur un brouillon jamais enregistré). Contrairement à "Réinitialiser" (purement local), celle-ci appelle bien le backend après confirmation ; `HouseholdSpatialView.jsx` sort du Mode Édition ensuite (vers un autre étage, ou l'accueil s'il n'en reste plus).

### 3.3 — Vue spatiale 2D/3D (navigation, hors édition)

- Commutateur à 2 positions dans l'en-tête : **"✏️ Plan 2D"** (éditeur) / **"🏠 Vue 3D"** (`FloorView3D`, vue de déplacement/navigation).
- Les deux vues lisent le **même layout sauvegardé**, chacune le rendant différemment — pas de duplication d'état ni de double logique de sauvegarde.
- **Plan2DView adapté au modèle mur-arête (01/08/2026)** : reçoit maintenant `tiles` (sol uniquement) **et** `edges` (murs/ouvertures), rendus en surimpression via un composant partagé `WallEdges.jsx` (SVG). **Correction (même jour)** : `LayoutEditor.jsx` utilise désormais aussi `WallEdges` (arêtes recalculées en direct depuis l'état courant des pièces/ouvertures, y compris pendant un déplacement/redimensionnement) — avant, l'éditeur n'affichait que la bordure CSS individuelle de chaque pièce, insensible au concept d'ouverture, donc "retirer un mur" n'avait visuellement aucun effet tant qu'on n'avait pas enregistré et consulté le Plan 2D. `WallEdges` est maintenant la SEULE source de vérité visuelle des murs, partagée entre édition et lecture seule. **`FloorView3D` PAS encore adapté** — toujours sur l'ancien modèle tile-based, priorité donnée au 2D d'abord (demande explicite). Conséquence connue et acceptée temporairement : une fois `FloorView3D`/`pathfinding.js` alimentés par des `tiles` qui ne contiennent plus de dalles "wall"/"door", l'avatar pourra traverser un mur sans ouverture (le pathfinding actuel ne connaît que le type de dalle, pas les arêtes) — à corriger avant de rouvrir ce chantier, voir `TODO.md`.
- **Vue détaillée de pièce + filtre propreté (03/08/2026, demande explicite de Paul, prototypes `ui_2d_v0.3.1.html`/`ui_room_v0.2.1.html`)** :
  - **Changement de comportement au clic** : une pièce dans `Plan2DView.jsx` n'ouvre plus la Vue 3D — elle bascule vers **`RoomDetailView.jsx`** (nouveau composant), avec un bouton "Retour au plan 2D" pour revenir. Câblé dans `HouseholdSpatialView.jsx` (`detailRoomId`/`handleBackToPlanFromDetail`) : quand une pièce est en détail, le sélecteur d'étage et le commutateur Plan 2D/Vue 3D sont masqués (remplacés entièrement par `RoomDetailView`), pas juste superposés.
  - **Murs blancs 2px** (prototype : `border-2 border-white` par pièce) : **repris différemment**, pas à l'identique — un contour par pièce aurait réintroduit le bug déjà corrigé (ignorance des ouvertures). `WallEdges.jsx` gagne un paramètre `strokeColor` (+ `wallThickness` déjà existant), Plan2DView passe blanc/2px ; `LayoutEditor.jsx` garde son gris foncé par défaut, inchangé.
  - **Filtre propreté (heatmap)** : état local `showHeatmap` dans `Plan2DView.jsx`, bascule entre couleur neutre (`room.color`) et couleur thermique dérivée de `room.activeTaskCount` (0=vert/1=jaune/2=orange/3+=rouge, échelle fixée par Paul). Deux tokens ajoutés à `theme.css` (`--color-yellow`, `--color-orange`) pour compléter l'échelle à 4 niveaux (emerald/rose existaient déjà). Légende affichée quand le filtre est actif. Masqué en mode compact (`showHint=false`, utilisé par `HouseholdHomePage.jsx`).
  - **`Plan2DView.css` basculé sur les variables de `theme.css`** (demande explicite de Paul) : gardait des couleurs codées en dur pensées pour un fond clair (`#eef0e9`, `rgba(0,0,0,0.6)`), sans lien avec le thème sombre réel de l'app — `--bg-app` (`#090d16`) correspond d'ailleurs exactement au fond du canevas du prototype, confirmant que c'était la bonne teinte à utiliser depuis le début.
  - **`RoomDetailView.jsx`/`.css` (nouveau)** : nom/type/icône/couleur/surface réels (mêmes données que `Plan2DView.jsx`) ; jauge de propreté, "Dernier ménage", "Gains potentiels", liste de tâches et historique en **données placeholder** (aucun service `tasks` n'existe encore, même situation que `HouseholdHomePage.jsx`) — cases à cocher interactives en état local uniquement (rien n'est persisté), filtres ("Toutes"/"Pour moi"/"Ménage") fonctionnels sur ces données locales. Icônes : `components/ui/Icons.jsx` pour toute la nouvelle interface (flèche retour, gemme, plus, checklist) ; l'icône de TYPE de pièce reste l'emoji existant (`roomTypes.js`), système distinct et déjà établi, non concerné par cette demande.
  - **Vérifié** par compilation réelle (`esbuild`) sur les 5 fichiers `.jsx` touchés/créés. **Non vérifié en navigateur réel** (rendu visuel, interaction tactile des filtres/cases à cocher).
- **Gestion des étages (01/08/2026, demande explicite de Paul ; ajustée le 02/08/2026)** : "🏢 Ajouter un étage" — **déplacé le 02/08/2026** de l'écran de consultation du plan vers `LayoutEditor.jsx` (c'est une action D'ÉDITION, demande explicite de Paul) : bouton + confirmation (si brouillon non vide) vivent maintenant dans la barre d'outils de l'éditeur, qui vide son propre brouillon puis prévient le parent via `onAddFloor`. Repart sur un plan vierge en Mode Édition, le prochain enregistrement crée un nouvel étage numéroté après ceux déjà présents (`handleSaveLayout` calcule un `floorMeta` sensé — corrige au passage un bug latent où un nouvel étage retombait TOUJOURS sur "Rez-de-chaussée"/RDC/niveau 0). "🗑️ Supprimer cet étage" **également déplacé** (02/08/2026, même raison) dans `LayoutEditor.jsx` — supprime UN étage **déjà enregistré** côté serveur, sans toucher aux autres étages. "🗑️ Réinitialiser le plan" **renommé** (02/08/2026, était "Réinitialiser cet étage" — demande explicite de Paul, redondant avec "Supprimer cet étage" désormais voisin) : vide uniquement le brouillon local, aucun appel backend (voir plus bas).
- **Après enregistrement (02/08/2026, demande explicite de Paul — remplace une demande antérieure)** : bascule sur le **Plan 2D** (`setViewMode("2d")`), pas la Vue 3D — l'utilisateur voit d'abord l'ensemble du plan qu'il vient de dessiner.
- **Plan2DView : un rectangle par pièce, pas par dalle (02/08/2026, demande explicite de Paul, exemple visuel à l'appui)** : remplace le rendu par dalle (`.plan2d-tile`, une bordure fine par case → quadrillage visible sur toute la pièce) par UN SEUL rectangle de couleur par pièce (dérivé directement de `{x,y,width,height}`), avec nom + nombre de tâches actives affichés dessus, centrés. `tiles` n'est plus utilisé du tout par ce composant. **Tâches actives = placeholder** : `room.activeTaskCount` n'existe dans aucune donnée réelle (le chantier Tâches n'a pas commencé), retombe systématiquement sur 0/"Aucune tâche" — voir `TODO.md`. Murs/ouvertures restent rendus par `WallEdges` (pas un contour uniforme par pièce, qui ignorerait les ouvertures).
- **Quadrillage de l'éditeur rendu plus visible (02/08/2026, demande explicite de Paul — "l'inverse" du quadrillage voulu sur le plan)** : `LayoutEditor.css` avait déjà un quadrillage (`linear-gradient`) mais trop discret (`rgba(0,0,0,0.12)`) pour être remarqué — opacité doublée (`0.22`).
- **Plan2DView : ajustement automatique à l'écran + zoom (02/08/2026, demande explicite de Paul, capture d'écran à l'appui)** : `CELL_PX` fixe (40px) faisait dépasser le conteneur dès que le plan avait plusieurs pièces — recadrage illisible en bord d'écran (comparé à `LayoutEditor.jsx`, cellule fixe plus petite à 28px, qui montrait tout sans ce problème). `cellPx` est maintenant dérivé de la taille RÉELLE du conteneur (mesurée via `ResizeObserver`) divisée par les dimensions de la grille de l'étage — tout l'appartement tient par défaut, quelle que soit sa taille (borné entre 14px et 64px pour rester lisible/raisonnable aux extrêmes). Boutons +/- ("Tout voir" pour revenir à l'ajustement automatique) ajoutés — un simple multiplicateur sur `cellPx`, PAS de bibliothèque de zoom tierce (cohérent avec la décision déjà prise d'éviter `react-zoom-pan-pinch`, voir plus haut dans ce document).
- **Recadrage dynamique sur la bounding box des pièces (03/08/2026, demande explicite de Paul, capture d'écran à l'appui — correction du point précédent)** : le calcul ci-dessus divisait par `floor.gridWidth`/`floor.gridHeight` (la grille COMPLÈTE disponible pour tracer, ex. 20×16 par défaut dans `LayoutEditor.jsx`) plutôt que par l'emprise réelle des pièces — un grand bloc vide apparaissait là où aucune pièce n'existe (visible sur la capture : fond gris à droite). Calcule maintenant `minX`/`minY`/`maxX`/`maxY` à partir de `rooms` à chaque rendu, plus 1 case de marge de chaque côté (`BBOX_PADDING_CELLS`) — coût négligeable (quelques pièces par étage). Pièces ET arêtes (`WallEdges`, nouveau paramètre `offsetXPx`/`offsetYPx`, défaut 0 = comportement inchangé pour `LayoutEditor.jsx`) décalées de l'origine de cette bounding box, pas de la grille complète. `.plan2d-scroll` centre maintenant son contenu (flex) quand il tient dans le cadre — défilement natif conservé si un zoom manuel dépasse. Logique de bounding box vérifiée par simulation (Node, 18 assertions sur une géométrie à 4 pièces proche de la capture) avant d'être écrite ici.
- **Zoom manuel entièrement retiré (03/08/2026, demande explicite de Paul)** : après plusieurs allers-retours pour corriger le comportement du zoom (bornes, bandes grises), Paul a finalement demandé de retirer complètement l'option +/- — la vue affiche maintenant TOUJOURS l'ajustement automatique ("Tout voir"), sans état ni bouton de zoom. Plus simple ; l'ajustement automatique était de toute façon déjà le seul niveau vraiment utile (voir les deux entrées précédentes). `overflow: auto` conservé comme filet de sécurité pour un très grand appartement où `MIN_CELL_PX` empêcherait le contenu de tenir entièrement à l'écran.
- **"🗑️ Réinitialiser cet étage" (dans `LayoutEditor.jsx`) — corrigé deux fois le même jour (01/08/2026)** : ~~appelait `onReset` (prop), qui effaçait TOUT LE FOYER côté backend via `resetHouseholdLayout` puis rebasculait vers l'écran d'accueil~~ **1ère correction** : reste en Mode Édition sur un plan vierge plutôt que de rebasculer vers l'accueil. **2e correction, "ne fait rien" signalé par Paul** : le bug persistait quand même — `useState(existingRooms)` dans `LayoutEditor.jsx` ne se resynchronise jamais avec des props qui changent sans démontage (pas de `key` côté parent), donc le brouillon affiché à l'écran ne changeait JAMAIS, quoi que fasse le backend derrière. Solution retenue : plus aucun appel backend ni prop `onReset` — le bouton vide directement l'état local de `LayoutEditor` (pièces, ouvertures, tout l'état d'interaction en cours), exactement comme "Annuler" ne persiste rien tant que "Enregistrer le plan" n'est pas cliqué explicitement ensuite. Portée limitée à l'étage en cours d'édition, jamais aux autres.
- L'ancienne "Vue Ensemble" en cartes (`ApartmentOverview2D.jsx`) a été supprimée : redondante avec le Plan 2D qui montre déjà toutes les pièces vues du dessus.

### 3.4 — Tâches & Mobilier (en pause volontaire)

- Une première version complète existait (tiroir de meuble, création de tâche multi-pièces, récurrence, heatmap de progression par pièce) mais a été **retirée intentionnellement** du flux actif pour stabiliser d'abord l'éditeur de plan (déplacement, aimantage, portes) sur une base spatiale solide.
- Rien n'est perdu : la logique reste documentée dans l'historique de conversation pour reprise ultérieure.
- Le modèle de tuiles connaît déjà un type `furniture`, mais aucun éditeur de mobilier n'est actif aujourd'hui.

### 3.5 — Rôles PROPRIETAIRE/LOCATAIRE (frontend)

- `useHouseholdRole(householdId)` : hook qui résout `{ role, isOwner, loading }` pour le compte connecté sur un foyer donné, à partir du modèle `occupants` + `claimedByUserId` + `role` déjà en place côté backend.
- Branché dans `HouseholdViewPage.jsx` → transmis à `HouseholdSpatialView.jsx` :
  - le bouton "Modifier le plan" est masqué pour un LOCATAIRE ;
  - si le plan est vide et que le compte n'est pas propriétaire, un message d'attente s'affiche plutôt que l'écran de création (qui échouerait de toute façon côté backend).
- **Le backend reste la seule vraie barrière de sécurité** — ce hook n'ajuste que l'affichage ; un contournement frontend serait de toute façon refusé côté serveur.

### 3.6 — Réorganisation Frontend (architecture Feature-Based)

- Passage d'une organisation "par type de composant" à une organisation **par fonctionnalité métier** : `src/features/auth/`, `src/features/household/`, `src/features/layout-editor/`.
- 48 fichiers déplacés, tous les imports audités et corrigés (`src/` et `tests/`).
- **`src/features/tasks/` (03/08/2026, demande explicite de Paul)** : `features/household/` atteignait 58 fichiers (+ son dossier `utils/`) — extraction de `taskService.js`, `useHouseholdTasks.js`, `useHouseholdRoomNames.js`, `HouseholdTasksPage.jsx/.css` dans un nouveau dossier dédié, même logique que l'extraction antérieure de `layout-editor/`.
- ~~**`RoomDetailView.jsx` reste dans `household/`**~~ **Corrigé le jour même (voir entrée suivante) : a finalement bougé lui aussi**, mais vers `floor-plan/`, pas vers `tasks/` (voir ci-dessous).
- **`src/features/floor-plan/` (03/08/2026, même session, demande explicite de Paul — "d'autres fichiers à réorganiser ?")** : le bloc spatial, de loin le plus gros morceau de `household/` (`HouseholdSpatialView.jsx` seul faisait 23,7 Ko, le plus gros fichier du projet), extrait à son tour — `HouseholdSpatialView.jsx/.css`, `HouseholdSpatialPage.jsx`, `Plan2DView.jsx/.css`, `FloorView3D.jsx/.css`, `RoomDetailView.jsx/.css`, `OnboardingScreen.jsx/.css`, `floorPlanService.js`, `mockData.js`, `utils/pathfinding.js`. **Zéro import interne à corriger** : chaque référence entre ces fichiers était déjà relative à un voisin de même dossier ou à un autre `features/*` de même profondeur (`layout-editor/`, `tasks/`, `auth/`, `components/ui/`, `lib/`) — le déplacement en bloc ne casse donc rien à l'intérieur. 3 imports corrigés côté appelants externes : `AppRouter.jsx` (`HouseholdSpatialPage`), `HouseholdHomePage.jsx` (`floorPlanService`/`Plan2DView`, resté dans `household/` mais consommant le plan), `useHouseholdRoomNames.js` (`floorPlanService`, `features/tasks/`).
- **`UserMenu.jsx/.css` déplacé vers `features/account/`** (même session) : c'est un menu de compte (profil/réglages/déconnexion), pas un fichier de foyer — 1 import corrigé (`AppLayout.jsx`).
- **Suppression massive de code mort confirmée par Paul (03/08/2026)** — ~~"Certains fichiers de l'ancien backend... sont volontairement restés en place, non déplacés/supprimés sans confirmation explicite"~~ **confirmation obtenue, tout supprimé**. Chaîne d'import réellement tracée depuis `AppRouter.jsx`/`main.jsx` (les deux seules vraies racines), pas supposée — voir `README.md` pour la liste complète (~30 fichiers/dossiers). Découverte notable au passage : `components/ui/` n'avait en réalité que **deux** fichiers actifs (`Icons.jsx`, `AmbientGlow.jsx`) — `Toast`/`Spinner`/`Skeleton`/`StatusBadge`/`ProgressBar`/`ItemCard`/`ItemForm`/`ItemGrid` étaient tous déjà morts, contrairement à ce qu'affirmait ce document ("génériques, toujours utilisés"). Deux fichiers documentés supprimés lors de révisions précédentes de `README.md` sans jamais l'être réellement ont aussi été trouvés et enfin supprimés pour de bon : `HouseholdViewPage.jsx/.css` et `features/household/Icons.jsx` (copie orpheline).

### 3.7 — Migration Supabase (routing + auth) — en cours

- `AppRouter.jsx` : vraies routes (`/login`, `/signup`, `/onboarding`, route protégée générale) avec gardes empilées `RequireAuth` → `RequireHousehold`.
- `AuthContext.jsx` (`AuthProvider`/`useAuth()`) : session Supabase partagée dans toute l'app, écoute `onAuthStateChange`, expose `signUp`/`signIn`/`signOut` au contrat `{ success, data?, error? }`.
- `RequireAuth.jsx` / `RequireHousehold.jsx` : gardes de route, mémorisent la page d'origine pour y revenir après connexion.
- `src/lib/supabaseClient.js` créé et câblé sur les variables d'environnement (`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`) — **mais aucune requête de données ne l'utilise encore**.
- Schéma Postgres + RLS complet écrit (`backend/supabase/migrations/01_schema_and_rls.sql`) : tables `users`, `households`, `household_members`, `occupants`, `floor_plans` (JSONB `layout_data`, source unique de vérité), `tasks`, `expenses` ; fonctions `security definer` (`is_household_member`, `get_household_role`) pour éviter la récursion RLS ; policies alignées sur la matrice de droits (lecture ouverte à tous les membres, écriture du plan réservée au PROPRIETAIRE).
- **Correction (voir §4) : ce schéma A bien été exécuté et testé sur un projet Supabase réel** — `03_fix_households_select_bootstrap.sql` documente un vrai bug de policy RLS trouvé en conditions réelles (relecture `.insert().select()` soumise à la policy SELECT, échouant tant que `household_members` n'a pas encore sa ligne), avec diagnostic détaillé et correctif. L'affirmation inverse plus bas dans une version antérieure de ce document était erronée.
- État transitoire connu : le composant historique conserve pour l'instant son propre écran de connexion interne (ancien backend), indépendant de Supabase — les deux coexistent le temps de la bascule.

### 3.8 — Design & Habillage visuel

- Thème sombre néon cohérent sur les écrans Connexion/Inscription, tableau de bord des logements, création de foyer — fond plein écran avec halos, pas de conteneurs encadrés (sauf les dialogues de confirmation, volontairement différenciés).
- Traduction systématique des maquettes Tailwind/FontAwesome fournies par Paul (`docs/ui/new/*.html`) en CSS classique — **Tailwind et FontAwesome ne sont jamais adoptés comme dépendances réelles**.
- Onboarding en accordéon à 3 options + 1 option secondaire ("Créer" / "Scanner un plan" / "Rejoindre un logement" / "Continuer sans foyer") : seule **"Créer un logement"** est fonctionnelle aujourd'hui, les deux autres sont visibles mais désactivées ("Bientôt").

### 3.9 — Coquille applicative & Accueil (`AppLayout`, thread de conversation séparé)

Travail mené dans un autre fil de conversation que celui ayant produit les sections précédentes — ajouté ici pour que ce document reste la trame unique inter-fils (voir tête de fichier). Construit *au-dessus* de la migration Supabase de la §3.7 (routing/auth), sans dépendre des blocs métier (plan, tâches) encore sur l'ancien backend. Ordre chronologique réel de ce fil :

1. **Restructuration du routing en routes imbriquées** : `/households/:householdId` devient une route PARENT montant `<AppLayout/>`, avec les écrans du foyer actif en routes enfants (`<Outlet/>`) plutôt qu'une seule page plate. `HouseholdViewPage.jsx` retiré du routing — son contenu (nom, code d'invitation, membres) correspondait en fait à l'onglet "Vie du foyer" de la spec, pas à l'Accueil (qui doit être un résumé) : renommé `HouseholdLifePage.jsx`. `TabPlaceholder.jsx` introduit comme habillage partagé pour les onglets pas encore implémentés (même logique que `PlaceholderModal.jsx`).
2. **Menu profil (`UserMenu.jsx`)** : remplace une cloche de notifications — "Profil"/"Réglages" en badge "Bientôt", "Se déconnecter" fonctionnel (`useAuth().signOut()`). Absent jusque-là : aucun moyen facile de se déconnecter.
3. **Correctif de redirection post-connexion** : une reconnexion ramenait sur la dernière page ouverte avant déconnexion (`LoginPage.jsx` utilisait `location.state.from`, mémorisé par `RequireAuth.jsx` au moment de l'interception) au lieu de l'Accueil. Corrigé : connexion → toujours `/households` ; `HouseholdDashboardPage.jsx` redirige maintenant automatiquement vers l'unique foyer d'un compte qui n'en a qu'un (implémente le cas N=1 de la spec, jusque-là non fait). **Effet de bord découvert et corrigé dans la foulée** : cette redirection auto rendait `/households` (et son "+ Créer un logement") injoignable pour un compte à un seul foyer — ajout de "+ Ajouter un logement" dans `HouseholdSwitcher.jsx` pour garder ce chemin ouvert.
4. **Accueil traduit du prototype** (`HouseholdHomePage.jsx`, `docs/ui/home/ui_home_v0.5.x.html`) en CSS classique (palette étendue : `--color-sky`/`--color-rose`/`--color-violet` ajoutés à `theme.css`). **Pièces réelles depuis le 03/08/2026** (voir journal) — restent en données statiques : tâches du jour, liste de courses, "Propreté globale", en attendant les services réels.
5. **`HouseholdSwitcher.jsx`** : remplace le `<select>` natif de l'étape 1 (rendu par l'OS, cassait l'esthétique glassmorphique) par un bouton + menu déroulant maison. Ajoute "Gérer mes logements" (`/households?manage=1`, contourne la redirection auto de l'étape 3) et une icône de foyer cliquable pour en changer la photo — **aperçu local uniquement (`URL.createObjectURL`), non persistant** (voir nuance §4 sur `households.avatar_url`, colonne inexistante à ce jour).
6. **6ᵉ onglet "Dépenses"** ajouté à `AppLayout.jsx` (la table `expenses` existe déjà en base, §3.7, mais aucun service frontend ne la consomme — `HouseholdExpensesPage.jsx` en placeholder). Cartes de l'Accueil rendues cliquables vers les onglets correspondants (Vue du foyer → Plan, Tâches ménagères/Tâches du jour → Tâches, Liste de courses → Dépenses).
7. **Badges streak/gemme (header)** deviennent de vrais boutons — flamme ouvre `StreakModal.jsx` (même habillage que `PlaceholderModal.jsx`), gemme mène à `/households/:id/rewards` (`HouseholdRewardsPage.jsx`, placeholder). Valeurs toujours à 0 (pas de colonnes `streak_count`/`points_balance` en base).
8. **Correction post-upload** : un routeur (`AppRouter.jsx`) et une feuille de style (`AppLayout.css`) modifiés pour l'étape 7 avaient été sauvegardés au mauvais endroit (`features/household/AppRouter.jsx` et `src/AppLayout.css`, au lieu de `src/AppRouter.jsx` et `src/features/household/AppLayout.css`) — la route `/rewards` n'était donc jamais réellement montée, et les styles de survol des badges jamais chargés. Fusionnés dans les bons fichiers ; doublons et un `HouseholdViewPage.jsx`/`.css` de l'étape 1, déjà remplacé mais non supprimé localement, supprimés.
- **⚠️ À répercuter dans `ROUTINGANDUSERFLOWS.md`** : ce document spécifiait 5 onglets avec les dépenses logées dans "Vie du foyer" — le code réel en a 6, dépenses séparées. Document pas encore mis à jour en conséquence.

### 3.10 — Profil, Réglages, Préférences & Calendrier réel

Intégration de 4 prototypes (`docs/ui/profile/`, `docs/ui/settings/`, `docs/ui/calendar/`, tous v0.1.2) suite à demande explicite de Paul. Nouveau dossier **`features/account/`** créé (pages de niveau *compte*, pas *foyer* — distinct de `features/household/`).

- **`/profile`, `/settings`, `/settings/preferences`** : nouvelles routes plein écran autonomes (bouton retour, pas de bottom nav — cohérent avec les prototypes Réglages/Préférences ; le prototype Profil, lui, affichait une bottom nav sans bouton retour, probablement un copier-coller non nettoyé du prototype Accueil — traité comme un écran autonome par cohérence, bouton retour ajouté). `UserMenu.jsx` : "Profil"/"Réglages" ne sont plus des items désactivés "Bientôt", ils naviguent réellement.
- **`SettingsPage.jsx`** : seuls "Profil utilisateur" (→ `/profile`) et "Notifications et Préférences" (→ `/settings/preferences`) mènent vers de vraies pages ; le reste (Foyers, Confidentialité, Réseaux sociaux, Cookies, Abonnement, Aide & FAQ, Contacter le support, mentions légales) reste inerte, badge "Bientôt". "Supprimer mon compte" désactivé délibérément (suppression irréversible, RGPD, cascade PROPRIETAIRE — un vrai chantier à part, pas une simple page). Barre de recherche réellement fonctionnelle (filtre les libellés affichés). Badge de version du prototype ("v0.5.3") volontairement non repris — c'est un numéro de fichier de maquette, pas une version d'app.
- **`PreferencesPage.jsx`** : interrupteurs (sons, vibrations, animations, mode sombre, messages de motivation, conseils, vue 3D, économie d'énergie, mode daltonien) en **état React local réel** (cliquables, réagissent visuellement) mais **rien n'est persisté** — aucune table de préférences utilisateur côté Supabase, tout se réinitialise au rechargement (même esprit que l'aperçu d'icône de foyer non sauvegardé, §3.9). "Mode sombre" reste affiché par fidélité au prototype mais n'a aucun effet réel (l'app est dark-mode uniquement par conception). Section "Notifications & Rappels" (6 items) entièrement inerte, aucune sous-page construite.
- **`ProfilePage.jsx`** (v0.1.1, la référence correcte — voir journal 02/08) : cadre avatar à bordure dégradée avec initiales en repli (dérivées de l'email) + bouton caméra (aperçu local uniquement). **Écarts assumés** : stats/badges affichés à zéro/vide plutôt que les chiffres de démo du prototype ("5 jours", "320 pts", "42 tâches", "3/8 débloqués") — même principe que les badges du header d'`AppLayout`, jamais simulés. Coche "Membre vérifié" retirée (aucune vérification d'identité n'existe). Badge "Membre ChezNous Plus" simplifié en "Membre" (aucun abonnement réel). "Membre de [foyer] depuis" adapté en date de création du compte (page non imbriquée dans un foyer précis).
- **`HouseholdCalendarPage.jsx`** (remplace le placeholder `TabPlaceholder`) : grille du mois **calculée dynamiquement** à partir de la vraie date du jour (contrairement au prototype, figé sur "Août 2026") — reste correcte quel que soit le mois où on l'ouvre. Filtres de catégorie et sélection de jour réellement fonctionnels. Vues "Jour"/"Semaine" affichent un message d'attente plutôt que de faire semblant (seule "Mois" est implémentée, comme dans le prototype lui-même). Événements mock positionnés par rapport à aujourd'hui (pas de dates en dur), pour qu'il y ait toujours quelque chose à voir. Aucune table `calendar_events` dans `DATAMODEL.md` — à créer quand ce chantier sera repris pour de vrai.
- **Palette étendue** : `--color-purple` et `--color-pink` ajoutés à `theme.css`. Le "bleu" de plusieurs icônes du prototype a été volontairement fondu sur `--color-sky` déjà existant (teintes visuellement proches, même rôle) plutôt que dupliqué.
- **~20 icônes ajoutées** à `components/ui/Icons.jsx` (flèche retour, loupe, partage, haut-parleur, téléphone, formes, lune, étoile, ampoule, baguette magique, batterie, cookie, carte, aide, trophée, camembert, balai, clé, confettis, coche-cercle, chevron gauche).

---

### 3.11 — Schéma Tâches V1 (Supabase, chantier 🅲)

Périmètre entièrement défini dans `PRODUCTVISION.md` §4 (section enrichie le 03/08/2026, toutes les questions ouvertes sur les Tâches y sont tranchées) — cette entrée documente uniquement le **schéma retenu et son exécution**, voir `DATAMODEL.md` §2/§7 pour le détail complet des colonnes.

- **Migration `backend/supabase/migrations/04_tasks_v1_schema.sql` proposée puis exécutée le 03/08/2026** — confirmée par Paul (capture d'écran du SQL Editor Supabase, script complet exécuté sans erreur avant l'exécution : "Success. No rows returned").
- **`tasks` enrichie** : `importance` (enum `BASSE`/`NORMALE`/`HAUTE`, informatif), `deadline_offset_days` (récurrentes), `task_group_id`, `depends_on_task_id` + `depends_on_every_n` (dépendance d'instance V1, informative — jamais bloquante).
- **`task_groups`** (nouvelle table) : remplace la piste `task_rooms` abandonnée — plusieurs instances de tâches (une par pièce), rattachées à un groupe commun, chacune avec son propre statut.
- **`task_assignees`** (nouvelle table) : remplace `tasks.assigned_to`/`assigned_to_occupant_id` (colonnes supprimées après migration des données existantes) — assignation à plusieurs comptes utilisateurs et/ou occupants non-utilisateurs simultanément, contrainte `CHECK` garantissant exactement une cible par ligne.
- **Deux décisions de schéma actées par défaut, Paul ayant exécuté le fichier tel quel proposé** : suppression d'un groupe → `ON DELETE SET NULL` (tâches conservées, dégroupées) ; pas de `CHECK` dur en base sur la mutuelle exclusivité `due_date`/`deadline_offset_days` (règle côté formulaire frontend pour l'instant).
- **Non fait à ce stade** : aucun service frontend ne consomme encore ce schéma — `HouseholdTasksPage.jsx` reste un placeholder (`TabPlaceholder`), `RoomDetailView.jsx` reste sur ses données mock locales (`TODO.md` #10/#11, toujours valides). Prochaine étape naturelle du chantier 🅲 : écrire `taskService.js` (Supabase) et brancher l'UI.

### 3.12 — Branchement frontend des Tâches (lecture + bascule de statut)

Suite de §3.11 (schéma exécuté) — même session, décision prise avec Paul : service d'abord (`taskService.js`), écran de création riche reporté à une session dédiée (groupe/assignation multi-personnes/dépendance/récurrence, périmètre complexe, mérite sa propre conception).

- **`taskService.js` (nouveau)** : `listHouseholdTasks` (lecture enrichie — groupe + assignés résolus par NOM en une seule requête PostgREST imbriquée, `task_groups`/`task_assignees` → `users`/`occupants`, même technique que `householdService.js`), `createTask` (minimal — "ajout rapide" : titre + pièce, pas de groupe/assignation/récurrence), `updateTaskStatus` (bascule DONE/TODO). Volontairement PAS d'écriture sur `task_assignees`/`task_groups` — viendra avec l'écran de création.
- **`useHouseholdTasks.js`/`useHouseholdRoomNames.js` (nouveaux)** : hooks loading/error/reload, même convention que `useHouseholds.js`/`useHouseholdDetail.js`. Le second résout `{roomId: roomName}` via `floorPlanService.js` (même source que Plan2DView), utilisé pour grouper les tâches par pièce dans l'onglet Tâches.
- **`HouseholdSpatialView.jsx`** : un seul fetch de tâches pour tout le foyer (`useHouseholdTasks`), dérivé localement en compteur par pièce (`roomsOnSelectedFloorWithTasks`, passé à `Plan2DView` — ferme `TODO.md` #10) et en liste filtrée pour la pièce en détail (passée à `RoomDetailView` avec les callbacks de complétion/ajout rapide).
- **`RoomDetailView.jsx`** : liste réelle, case à cocher qui persiste vraiment (complète/rouvre), ajout rapide fonctionnel (`onAddQuickTask`). **Deux éléments du prototype retirés plutôt que faits à moitié** (même principe qu'ailleurs dans le projet — afficher honnêtement plutôt que fabriquer) : "Gains potentiels"/gemmes (gamification, chantier 🅳 pas commencé, aucune colonne de points) et "Dernier ménage" précis (remplacés par deux compteurs 100% réels, actives/terminées — voir `TODO.md` #12 : `tasks` n'a pas de `completed_at`/`completed_by`, impossible de savoir honnêtement qui/quand a coché en dernier). Filtre "Ménage" retiré aussi (aucun champ ne distingue un "type" de tâche aujourd'hui, aurait été redondant avec "Toutes"). Ferme `TODO.md` #11 (largement, pas à 100% — voir écarts assumés).
- **`HouseholdTasksPage.jsx`/`.css` (nouveaux, remplacent `TabPlaceholder`)** : vraie liste des tâches du foyer, groupées par nom de pièce. **Périmètre volontairement minimal** : pas de création depuis cet écran (utiliser l'ajout rapide de `RoomDetailView` en attendant) ; aucun prototype Tailwind ne couvre cet écran contrairement aux autres onglets — présentation modeste avec les tokens existants, à affiner une fois un prototype fourni ou l'écran de création conçu.
- **Vérification** : fichiers JS purs (`taskService.js`, les deux hooks) vérifiés par `node --check` (syntaxe valide). Fichiers JSX relus manuellement — **pas d'accès npm/esbuild dans cet environnement cette fois** (réseau désactivé, `node_modules` jamais installé dans ce zip) contrairement aux sessions précédentes sur l'éditeur de plan. Non vérifié en navigateur réel.
- **Deux découvertes documentées dans `TODO.md`, pas traitées ici (hors périmètre de la demande, signalées plutôt que corrigées par anticipation)** :
  - `TODO.md` #12 : `tasks` sans `completed_at`/`completed_by` (voir ci-dessus).
  - `TODO.md` #13 : **régression trouvée en inspectant pour styliser `HouseholdTasksPage.jsx`** — `.card-glow`/`.ambient-glow*`/`.no-scrollbar` sont utilisées par le code (`AmbientGlow.jsx`, monté dans `AppLayout.jsx`) et documentées comme ajoutées à `theme.css` le 02/08/2026 (journal ci-dessous), mais **n'existent pas réellement** dans le `theme.css` de ce zip (46 lignes, uniquement des tokens de couleur). Les halos d'ambiance sont donc actuellement invisibles partout dans l'app, sans erreur. Pas réinventé de mémoire (risque de ne pas correspondre à l'original) — à vérifier côté dépôt réel de Paul.

### 3.13 — Refonte de l'éditeur de plan (`PlanEditorView.jsx`, remplace `LayoutEditor.jsx`)

Demande explicite de Paul (03/08/2026), prototype `ui_plan_editor_v0.3.0.html` fourni ("l'ancienne UI était peu gracieuse et peu fonctionnelle") — reproduction fidèle + étude des fonctionnalités pour les rendre réellement viables, pas juste visuelles.

- **Renommage complet** `LayoutEditor.jsx`/`.css` → `PlanEditorView.jsx`/`.css` (même dossier `features/layout-editor/`), même logique que les renommages précédents (2.5D→3D, ApartmentSpatialMvp→HouseholdSpatialView) — appliqué aux fichiers, à `HouseholdSpatialView.jsx` (import mis à jour) et à tous les commentaires historiques des fichiers voisins (`Plan2DView.jsx`, `RoomDetailView.jsx`, `mockData.js`, `floorPlanService.js`).
- **Grille 20px** (`CELL_PX`, était 28px) — alignement strict déjà garanti par construction (coordonnées toujours en cases entières).
- **Cadenas par pièce supprimé, remplacé par "Ajuster"** (bouton dock, mode global) : off (défaut) = tout en lecture seule ; on = toutes les pièces et séparations deviennent manipulables, taper une pièce la sélectionne (une seule à la fois, poignées + pilule contextuelle), taper une cloison (outil Séparation actif) la modifie. Sortir désélectionne et reverrouille tout. **Décision de Paul, remplace ma proposition initiale** ("sélection" seule, sans notion de mode global) — les deux points ("cadenas" et "Ajuster") allaient ensemble dans sa réponse.
- **Sélecteur d'étage en dropdown** avec **changement d'étage réel sans quitter l'éditeur** (`onSwitchFloor`, nouveau prop) — amélioration au-delà du prototype statique : nécessite un `key={selectedFloorId}` sur `<PlanEditorView>` pour forcer un remontage complet à chaque changement d'étage, sinon même piège de resynchronisation déjà rencontré une fois pour "Réinitialiser" (`useState(existingRooms)` ne se réexécute jamais sur un changement de props sans démontage). Confirmation demandée si un brouillon non vide serait perdu (même logique que "+ Ajouter un étage").
- **Undo/Redo réels** (pile d'historique `{rooms, doors}`) — n'existaient pas du tout avant. Un point d'entrée unique (`commit()`) pousse un instantané à chaque changement VALIDÉ (jamais pendant un glissé en cours, seulement au relâchement/à la validation).
- **Séparation Porte/Fenêtre/Passage RÉELLEMENT fonctionnelle, pas cosmétique** (demande explicite de Paul, confirmée avant de coder) — changement de MODÈLE DE DONNÉES, pas seulement d'UI :
  - `doors` gagne un champ `type` optionnel (`'door'`\|`'window'`\|`'passage'`, défaut `'passage'` — rétrocompatible, aucune réinitialisation nécessaire contrairement à l'ajout d'`orientation` en 01/08).
  - `layoutGeneration.js` (`computeRoomEdges`/`findOpenableWallSegments`), `WallEdges.jsx` (rendu — **fenêtre conserve le mur** + marqueur bleu ciel, **porte/passage le retirent**, porte avec marqueur ambré/passage sans aucun marqueur) et `validateLayout.js` (schéma Zod) mis à jour. Voir `DATAMODEL.md` §5 pour le détail complet.
  - **Bug systémique trouvé et corrigé en vérifiant EXHAUSTIVEMENT (grep sur tout le projet, pas une relecture partielle)** : le nouveau champ `type` était silencieusement tronqué à **8 endroits différents** qui reconstruisaient l'objet porte champ par champ sans lui (`floorPlanService.js` ×3 — lecture/écriture/retour de `saveFloorLayout` —, `HouseholdSpatialView.jsx` ×4 — `floorTiles`/`floorEdges`/props initiales de l'éditeur/export/import JSON —, `HouseholdHomePage.jsx` ×1 — aperçu du plan sur l'Accueil). Sans cette vérification systématique, la fonctionnalité aurait semblé fonctionner dans l'éditeur puis perdu son information à la sauvegarde, à l'export ou sur l'Accueil.
- **13 nouvelles icônes** ajoutées à `Icons.jsx` (même style plein/solide que les 51 existantes) : coche, étages, undo/redo (une seule icône, l'autre sens obtenu par `transform: scaleX(-1)` en CSS, même technique que `ArrowRightIcon` pour `RoomDetailView`), options (⋮), export/import, stylo, poubelle, porte, fenêtre, gomme, ajuster (4 flèches), mur (séparation). **Non vérifiées visuellement dans un navigateur réel** (pas d'accès réseau dans cet environnement) — même limite que la réécriture complète du 02/08/2026, à confirmer chez Paul.
- **Bouton "Tâches" du dock** : pièce sélectionnée → navigue vers l'onglet Tâches du foyer (`taskService.js`, chantier 🅲) — pas encore de filtre par pièce (`HouseholdTasksPage.jsx` ne l'accepte pas aujourd'hui), amélioration possible plus tard (voir `TODO.md`).
- **Conservé sans changement** : modèle mur-arête, résolution anti-chevauchement uniquement au relâchement (pas d'aimantage), `RoomCreateModal.jsx`/`RoomInspector.jsx` (contrat de props inchangé), ajout/suppression d'étage restant des actions d'édition.
- **Vérification** : fichiers JS purs (`layoutGeneration.js`, `validateLayout.js`, `floorPlanService.js`) vérifiés par `node --check`. `PlanEditorView.jsx` (le plus gros fichier du chantier, ~700 lignes) relu manuellement en entier, script Python maison pour vérifier exhaustivement tous les imports relatifs du projet ET toutes les icônes importées — zéro import cassé, zéro icône manquante. **Non vérifié en navigateur réel** (rendu visuel, interactions tactiles) — à confirmer chez Paul avant de considérer ce chantier terminé, comme toujours dans cet environnement sans accès réseau.

**Addendum (03/08/2026, même jour) — 3 corrections après le premier vrai test de Paul :**
- **"Ajuster" et "Pièce" deviennent deux outils EXCLUSIFS** (`activeTool: null|'adjust'|'piece'`, remplace le booléen `editMode`) : "Ajuster" bloque désormais le tracé d'une nouvelle pièce (ne le faisait pas avant — source de gestes accidentels signalée par Paul) ; "Pièce" devient un vrai toggle (avant, l'appui dessus activait juste `editMode` sans état "actif" propre, le tracé restant possible en permanence dès qu'on tapotait la grille).
- **Zoom (+/−/reset)** ajouté — n'existait pas du tout dans l'éditeur. Implémenté sur une taille de case EFFECTIVE (`effectiveCellPx = CELL_PX × zoomScale`) plutôt qu'un `transform: scale()` CSS, pour rester fiable avec le défilement natif du conteneur (`overflow: auto`) — un transform CSS a un comportement parfois incohérent avec `scrollWidth`/`scrollHeight` selon les navigateurs, pas vérifiable sans accès réseau ici.
- **Pan tactile natif réactivé** : Paul butait contre les bords du conteneur en ajoutant des pièces — cause identifiée : `touch-action: none` posé en dur sur la grille (nécessaire pour que le glisser-tracer fonctionne, mais bloquait tout pan tactile en permanence, y compris hors tracé). Devient dynamique : `touch-action: none` uniquement quand "Pièce" est l'outil actif, `pan-x pan-y` sinon. Une pièce suit la même logique côté "Ajuster" (bloque le pan seulement quand elle a un rôle actif : sélection/glisser).
- Vérification : script de comptage/équilibrage des balises JSX refait spécifiquement après restructuration du conteneur canvas (ajout d'un wrapper pour ancrer le zoom hors du flux défilant) — confirmé équilibré. Non vérifié en navigateur réel, comme le reste de ce chantier.

**2ᵉ addendum (03/08/2026, même jour) — 2e test réel de Paul, 3 nouveaux points :**
- **Quadrillage de fond invisible** : opacité des traits doublée (`rgba(255,255,255,0.07)` → `0.15`, `PlanEditorView.css`) — même ajustement qu'avait déjà dû subir une fois l'ancien `LayoutEditor.css` (`0.12` → `0.22`) : un quadrillage d'éditeur a besoin d'être nettement plus visible qu'un motif décoratif.
- **"Séparation" ne devait pas dépendre d'"Ajuster"** — corrigé : devient un 4ᵉ outil autonome (`activeTool` peut valoir `'separation'`), retrait du `disabled={activeTool !== "adjust"}`. Revirement assumé de la demande initiale de Paul ("Ajuster déverrouille pièces ET séparations ensemble") — à l'usage, ça n'avait pas de sens, Séparation est un outil de nature différente.
- **Pan tactile toujours bloqué en lecture seule malgré le 1er correctif — cause réelle trouvée en relisant le CSS plutôt qu'en supposant que le fix JS suffisait** : deux règles `touch-action: none` STATIQUES étaient restées dans `PlanEditorView.css` (`.plan-editor-view__grid` et `.plan-editor-view__room`), écrites lors du premier jet du fichier puis jamais retirées quand le contrôle dynamique a été ajouté en style inline au 1er addendum — une incohérence CSS/JSX, seul le JSX avait été corrigé. Retirées. **Deuxième cause corrigée par prudence, même si non confirmée comme la cause exacte** (pas d'accès navigateur pour trancher) : `.plan-editor-view__canvas-wrap` centrait son contenu en flex (`align-items`/`justify-content: center`) tout en étant `overflow: auto` — combinaison connue pour un comportement de défilement peu fiable selon les navigateurs quand le contenu déborde. Remplacé par un simple `padding`, comportement de défilement standard sans particularité.

---

## 4. Divergences connues entre la documentation et le code

**⚠️ Correction (voir la conversation) : les trois points ci-dessous étaient périmés.** Ils décrivaient l'état de `01_schema_and_rls.sql` seul, sans tenir compte de `02_reconcile_with_data_model.sql`, qui existe précisément pour les corriger. Les deux migrations sont bien présentes dans `backend/supabase/migrations/` — à vérifier si les deux ont réellement été *exécutées* sur le projet Supabase (le code des deux fichiers existe, mais ça ne garantit pas qu'un `02` a été appliqué après un `01` déjà en place ; à confirmer avec Paul).

- ~~`DATAMODEL.md` mentionne un champ `invite_code`... aucune colonne `invite_code`~~ → **Résolu par `02`** : `households.invite_code` ajouté, généré automatiquement (format `K9B-2X1`) par trigger.
- ~~`users.name` unique, pas de `display_name`/`avatar_url`~~ → **Résolu par `02`** : `users.name` renommé en `display_name`, `avatar_url` ajouté. `password_hash` reste volontairement absent de `public.users` (délégué à `auth.users` de Supabase — dupliquer un hash de mot de passe dans une table publique serait un vrai problème de sécurité, ce n'est pas un oubli).
  - **Nuance importante pour la fonctionnalité de photo/icône personnalisable évoquée récemment** (switcher de foyer, header) : cet `avatar_url` est sur **`users`** (photo de profil **personnelle**), pas sur `households`. Il n'existe **aucune colonne `households.avatar_url`** dans les migrations actuelles — c'est ce qu'il faudrait ajouter (+ un bucket Supabase Storage + policy RLS) pour rendre persistante l'icône de foyer, qui n'est aujourd'hui qu'un aperçu local non sauvegardé (voir §3.9 et §5).
- ~~`tasks.assigned_to`/`expenses.category`... distingue `assignee_user_id`~~ → **Résolu par `02`** : renommé en `assigned_to` (nom exact du doc) ; `expenses.category` ajoutée (`varchar(50)`, défaut `'GENERAL'`). Extensions volontairement conservées au-delà du doc : `tasks.assignee_occupant_id` (assigner une tâche à un occupant non-utilisateur, ex. un animal), `tasks.description`, `tasks.recurrence_days`.
- Le système d'invitation par `invite_code` est au cœur du user flow d'onboarding documenté, mais n'a aucune implémentation fonctionnelle actuelle côté frontend (placeholder désactivé) — la colonne et la génération existent désormais côté base (`02`), reste à câbler le service frontend.

---

## 5. Feuille de route — Phases jusqu'à la commercialisation

Restructuré (voir §7, journal) suite à une session de clarification produit approfondie — voir **`PRODUCTVISION.md`**, le nouveau document compagnon qui fige *ce que l'app doit faire* (les chantiers ci-dessous répondent à *comment y arriver techniquement*, et renvoient vers les questions ouvertes de la Vision quand une décision produit doit être prise avant de coder).

L'ordre de phase est une proposition de priorité, pas une obligation. Beaucoup de chantiers de Phase 2 ont une **question ouverte associée** (voir `PRODUCTVISION.md` §11, renuméroté depuis §10 le 03/08/2026 suite à l'ajout de §9 Sécurité/Domotique) qui doit être tranchée avant de commencer à coder ce chantier précis — pas besoin de toutes les résoudre d'un coup, juste avant d'attaquer le chantier concerné.

### Phase 1 — Fondations techniques (prérequis silencieux, pas visibles pour l'utilisateur)

#### 🅰️ Branchement Supabase des données métier — *le socle*
Tant que ce chantier n'avance pas, tout le reste tourne sur deux systèmes en parallèle (§2).
- ~~Câbler les services foyers/occupants/rôles sur Supabase~~ → **déjà fait** (`householdService.js`, vérifié en lisant le code — §2 corrigé en conséquence).
- ~~Reconnecter le plan 2D/3D~~ → **fait et validé par un test réel** (Paul a créé un plan, sauvegardé, rechargé — fonctionne). `floorPlanService.js` lit/écrit le blob `floor_plans.layout_data` en un seul appel avec verrou optimiste sur `version` (colonne déjà en base, jusque-là inutilisée) — remplace `householdLayoutApi.js` (ancien backend, CRUD par entité). Mêmes signatures de fonctions exactement : `HouseholdSpatialView.jsx` n'a changé qu'une ligne (son import). Monté sur `/spatial` via `HouseholdSpatialPage.jsx` — le composant gère lui-même la bascule vers son mode édition via son propre bouton "Modifier le plan" (masqué pour LOCATAIRE), donc pas besoin d'une route `/editor` séparée pour que ça fonctionne ; une sous-route dédiée reste une amélioration mobile possible plus tard (chantier 🅲), pas un bloquant. `api.js`/`householdLayoutApi.js` n'ont plus aucun importeur actif, confirmé.
  - **Vue 3D déprioritisée (03/08/2026, décision explicite de Paul)** : "je la mets complètement de côté pour l'instant [...] elle viendra à la toute fin, je ne pense vraiment pas qu'elle soit importante" — Paul envisage même de la retirer entièrement de l'app (pas tranché, juste évoqué). Concrètement : `FloorView3D.jsx`/`pathfinding.js` restent tels quels (voir régression connue ci-dessus, `TODO.md` #4, priorité rabaissée en conséquence) — le code n'est PAS supprimé pour l'instant, juste laissé de côté. Le Plan 2D (`Plan2DView.jsx`, `LayoutEditor.jsx`, `RoomDetailView.jsx`) reste la priorité absolue du chantier spatial.
- Câbler tâches/dépenses/calendrier sur Supabase — aucune couche de données n'existe pour l'instant (ni ancien backend, ni Supabase : placeholders statiques uniquement, §2).
- **Suppression cascade / transfert de propriété obligatoire avant départ** (§3.1 — déjà implémenté côté ancien backend Node.js via `isHouseholdOwner()`) : à reporter côté Postgres sous forme de **triggers**, pas seulement de logique applicative React/service — sinon la règle n'est plus garantie une fois qu'on cesse de passer par l'ancien backend.
- Confirmer que `02_reconcile_with_data_model.sql` et `03_fix_households_select_bootstrap.sql` (§4) ont bien été *exécutées* sur le projet Supabase réel, pas seulement écrites dans le dépôt.
- **Bloque partiellement toute la Phase 2.**

#### 🅱️ Unification de l'authentification
- Retirer l'écran de connexion interne historique (ancien backend) une fois 🅰️ suffisamment avancé — les deux ne doivent plus coexister (§2, §3.1).
- **Dépend de** : 🅰️.

---

### Phase 2 — Boucle produit fonctionnelle (MVP utilisable de bout en bout)

#### 🅲 Modèle spatial → tâches, multi-pièces
- ~~**Décision de modèle de données à prendre en premier** : `tasks.room_id` est aujourd'hui une colonne unique (une pièce par tâche) ; passer à "une tâche peut couvrir plusieurs pièces" (`PRODUCTVISION.md` §4) demande une vraie table de jonction `task_rooms`, pas un simple changement de colonne.~~ **Résolu le 03/08/2026** : `task_rooms` abandonné au profit du concept de "groupe de tâches" (`PRODUCTVISION.md` §4, mis à jour en conséquence) — plusieurs instances de tâches (une par pièce), rattachées à un groupe commun, chacune avec son propre état d'avancement. `tasks.room_id` reste une colonne unique, inchangée.
- ~~**Périmètre V1 détaillé dans `PRODUCTVISION.md` §4**~~ **Schéma exécuté le 03/08/2026** (`04_tasks_v1_schema.sql`, voir §3.11/`DATAMODEL.md` §7) : `tasks` enrichie (importance, deadline_offset_days, dépendance d'instance, groupe), `task_groups`, `task_assignees` (assignation multi-personnes, remplace les colonnes uniques). Dépendances de groupe/bloquantes restent explicitement en V2.
- ~~**Reste à faire** : écrire le service frontend Supabase (`taskService.js`...)~~ **Fait le 03/08/2026** (§3.12) : `taskService.js` + `useHouseholdTasks.js`/`useHouseholdRoomNames.js`, branchés sur `HouseholdSpatialView.jsx` (compteur par pièce, `RoomDetailView.jsx`) et `HouseholdTasksPage.jsx` (remplace `TabPlaceholder`). Périmètre : lecture enrichie + bascule de statut + ajout rapide (titre + pièce) — pas encore d'écriture sur `task_assignees`/`task_groups` en tant que telle.
- **Reste à faire** : l'écran de création RICHE (groupe, assignation multi-personnes, dépendance, récurrence, importance) — chantier à part, pas encore commencé, mérite sa propre session de conception. Réintégrer aussi le mobilier dans le flux actif une fois les tâches bouclées (§3.4).
- **Dépend de** : 🅰️.

#### 🅳 Dette de ménage, notifications & gamification réelle
- **Dette de ménage** (`PRODUCTVISION.md` §4) : aucun modèle actuel — décider si elle est portée par l'utilisateur assigné ou le foyer (question ouverte #2), puis concevoir le calcul (tâche en retard = +1 dette, à quel moment exactement pour une récurrente : à la deadline ratée ou à la prochaine occurrence ?).
- **Canal de notification** (question ouverte #3) à trancher avant de commencer : in-app / push (service tiers) / email / combinaison — impact d'infra très différent selon le choix.
- Traduire "gamification" en mécaniques concrètes : `streak_count`/`points_balance` (colonnes absentes des deux migrations actuelles), logique de calcul, historique des tâches accomplies. `StreakModal.jsx`/`HouseholdRewardsPage.jsx` existent déjà comme coquilles UI (§3.9) — ce chantier leur donne du contenu réel.
- **Dépend de** : 🅰️, 🅲 (le calcul de dette/streak a besoin du modèle de tâches final).

#### 🅴 Dépenses, courses & vue d'ensemble "Vie du foyer"
- **Décision de modèle à prendre en premier** : Courses (liste collaborative, cochable, "pas encore acheté") et Dépenses (montant payé, par qui) sont probablement **deux modèles distincts** (`shopping_items` vs `expenses`), pas un seul — voir question ouverte #4. À trancher avant de créer les tables, sinon risque de devoir tout refaire.
- Dépenses récurrentes (loyer, abonnements) vs ponctuelles — étendre `expenses` sur le même principe que `tasks.recurrence_days` (précédent déjà en base, à généraliser plutôt qu'inventer un nouveau pattern).
- Construire le tracker de dépenses partagées (table + RLS existent, §3.7 — `HouseholdExpensesPage.jsx` est un placeholder, §3.9).
- Vue d'ensemble "Vie du foyer" : qui a fait quelles dépenses / quelles tâches (`PRODUCTVISION.md` §5).
- Notes spatiales (aucun code existant, seulement mentionnées dans la matrice de droits) — plus petit, peut se glisser ici ou être repoussé en Phase 3.
- **Dépend de** : 🅰️ ; 🅳 pour la partie "qui a fait ses tâches" de la vue d'ensemble.

#### 🅵 Onboarding complet & invitations
- Décider ce que voit un compte à 0 foyer qui choisit de "passer" la création (question ouverte #7) — le routing actuel force l'onboarding, il n'y a pas de vrai "skip" aujourd'hui.
- Split `/onboarding/select` (accordéon Créer/Scanner/Rejoindre) + `/onboarding/join` — actuellement une seule route plate (§3.8).
- Scan d'un dessin de plan (déjà identifié comme chantier "Scan de plan (IA)", toujours un placeholder) — gros morceau, nécessite un choix de service IA.
- Rejoindre par QR code ou email d'invitation (`PRODUCTVISION.md` §8) : à confirmer que ce sont deux *canaux* pour transmettre le même `invite_code` déjà en base (question ouverte #8), pas trois systèmes séparés. Email = service d'envoi transactionnel à choisir ; QR = génération + scan caméra, deux briques distinctes.
- Mettre à jour `ROUTINGANDUSERFLOWS.md` : toujours à 5 onglets (dépenses dans "Vie du foyer"), alors que le code en a 6 depuis §3.9.
- **Dépend de** : 🅰️ pour l'invite_code réel ; indépendant pour le reste.

---

### Phase 3 — Différenciation & robustesse (après un MVP qui tourne)

#### 🅶 Invités & séjours temporaires (statut exploratoire)
Le plus gros bloc de questions ouvertes du document Vision (`PRODUCTVISION.md` §7) — **ne pas commencer à coder avant d'avoir clarifié au moins** : compte ou occupant simplifié, durée fixe ou libre, tâches auto-créées ou deadline coïncidente, portée exacte du cloisonnement, et surtout si on parle d'un vrai système de location courte durée (réservation, calendrier de dispo, paiement) ou d'un statut d'occupant allégé — les deux ampleurs de chantier sont très différentes.

#### 🅷 Agenda / Calendrier
- Partagé foyer, par occupant, ou les deux (question ouverte #5) — pas assez de matière pour trancher seul, à décider ensemble.
- **UI réelle construite depuis §3.10** (grille du mois dynamique, filtres, sélection de jour) — reste un vrai service Supabase (table `calendar_events` à créer, absente de `DATAMODEL.md`) pour remplacer les événements mock.

#### 🅸 Icône de foyer personnalisable — persistance
Petit chantier isolé, peut se faire en parallèle de n'importe lequel des précédents.
- Colonne `households.avatar_url` (n'existe pas — à ne pas confondre avec `users.avatar_url`, déjà ajoutée par `02` mais pour la photo de profil **personnelle**, §4).
- Bucket Supabase Storage dédié + policy RLS (upload réservé au PROPRIETAIRE ?), service d'upload frontend pour remplacer l'aperçu local actuel (`HouseholdSwitcher.jsx`, §3.9).

#### 🅹 Dette technique
- Séparer prénom/nom (actuellement concaténés en un seul champ `name`/`display_name` partout).
- Multi-foyer réel avec "dernier foyer actif" persisté (confirmé comme intention produit, `PRODUCTVISION.md` §2 — actuellement un seul foyer actif à la fois, retombe sur une simple liste, §3.9).
- Connexion Google (OAuth) — bloqué côté Paul : nécessite d'abord un projet Google Cloud Console.

#### 🅺 Sécurité & Domotique spatiale (vision, pas encore de code)
Deux idées de Paul (03/08/2026), documentées en détail dans `PRODUCTVISION.md` §9 — vision long terme, explicitement postérieure à un MVP tâches/plan qui tourne (Phase 1/2 actuelles).
- **Vue "Sécurité"** (§9.1) : toggle spatial à côté de "Propreté", statut ouvert/fermé par ouverture. V1 déclaratif (l'utilisateur bascule lui-même) avant tout capteur réel. **Bloqué par une décision de modèle non triviale** : le champ `doors` actuel représente un mur *retiré en permanence*, pas un objet porte/fenêtre avec un état qui varie — nécessitera probablement un champ `type` (`opening`/`door`/`window`) et une conception des fenêtres depuis zéro (absentes du schéma aujourd'hui), pas un simple ajout de colonne `isOpen`.
- **Électroménager connecté** (§9.2) : déclenchement/notification de tâches depuis de vrais appareils (aspirateur robot, lave-vaisselle...). Séquencer réactif (lecture seule, webhook fin de cycle → tâche auto-créée) avant actif (déclenchement à distance, plus de risque). Candidats API réalistes : Home Connect, Tuya, SmartThings ; Roborock plutôt qu'iRobot (API grand public fermée côté iRobot). Recommandation : un point d'intégration unique (Home Assistant) plutôt que des connecteurs par marque.
- **Dépend de** : rien techniquement pour l'instant (vision pure) — dépendra du modèle de tâches (🅲, déjà fait pour la partie électroménager) et d'une décision de modèle d'ouverture (à instruire avant de coder la partie sécurité).

---

### Phase 4 — Commercialisation

Pas que du code — des décisions produit/business à anticiper, même si elles ne se codent pas tout de suite.

- **Modèle de pricing** : gratuit avec limites (nb de foyers/occupants ?) + palier payant ? Abonnement par foyer ou par utilisateur ? À définir avant d'implémenter la facturation.
  - **Brouillon de départ** *(repris d'une réponse de Gemini au même brief — une base de discussion, pas une décision arrêtée)* : palier Gratuit = 1 foyer + fonctionnalités de base ; palier Premium = foyers illimités, mode Invité avancé (dépend du chantier 🅶, encore à définir), export comptable des dépenses, scan de plan par IA. Cohérent avec les chantiers déjà identifiés comme "gros morceaux" (scan IA, invités) — logique de les réserver au palier payant plutôt que de les développer sans modèle de monétisation en tête.
- **Facturation** : intégration Stripe (ou équivalent) une fois le modèle de pricing tranché.
- **Conformité RGPD** : des données de foyer (dépenses partagées, présence d'occupants, potentiellement de mineurs si usage familial) sont des données personnelles sensibles — CGU, politique de confidentialité, droit à l'export/suppression des données. À anticiper, pas à improviser en dernière minute vu le public visé (familles).
- **Distribution** : rester en PWA mobile-first, ou packager en app native (App Store / Play Store) ? Impact sur les notifications push (question ouverte #3) si natif.
- **Support & feedback** : canal de retour utilisateur (même minimal) avant tout lancement public.
- **Observabilité** : sauvegardes Supabase, monitoring d'erreurs (Sentry ou équivalent), avant d'avoir de vrais utilisateurs non-Paul.

---

## 6. Backlog — Idées, correctifs & décisions à venir

*(Section à compléter au fil des prochaines sessions — chaque entrée peut préciser : date, description, bloc concerné, statut.)*

| Date | Idée / Correctif | Bloc concerné | Statut |
| :--- | :--- | :--- | :--- |
| — | — | — | — |

---

## 7. Journal des mises à jour de ce document

| Date | Changement |
| :--- | :--- |
| 31/07/2026 | Création initiale du fichier — état des lieux complet à partir de la documentation et du code existants. |
| 31/07/2026 | Ajout §3.9 (travail AppLayout/Accueil d'un autre fil, absent de la version initiale). Correction §3.7 et §4 : la section "Divergences connues" avait été écrite en ne lisant que `01_schema_and_rls.sql`, sans voir que `02_reconcile_with_data_model.sql` (déjà dans le dépôt) résolvait déjà ces mêmes écarts ; de même, `03_fix_households_select_bootstrap.sql` prouve que le schéma RLS a bien été testé en réel, contrairement à ce qu'affirmait §3.7. Corrigé un routeur et une feuille de style sauvegardés au mauvais endroit (voir §3.9) et supprimé 2 fichiers orphelins. |
| 31/07/2026 | Complété §3.9 avec deux étapes manquantes de ce fil (restructuration initiale du routing en routes imbriquées, correctif de redirection post-connexion vers l'Accueil). Transformé §5 d'une liste plate en feuille de route par chantiers priorisés avec dépendances explicites (🅰️ à 🅵). |
| 31/07/2026 | Création de `PRODUCTVISION.md` (document compagnon, fige les objectifs/fonctionnalités de l'app suite à une session de clarification produit avec Paul) et restructuration complète de §5 en 4 phases (Fondations → Boucle produit fonctionnelle → Différenciation/robustesse → Commercialisation), avec renvois vers les questions ouvertes de la Vision produit qui doivent être tranchées avant certains chantiers. |
| 31/07/2026 | Ajout de deux idées reprises d'une réponse de Gemini au même brief (triggers Postgres pour la cascade/transfert de propriété dans 🅰️, brouillon de pricing Gratuit/Premium en Phase 4) — les autres apports de Gemini (équilibrage des dépenses, proposition Invités) ajoutés côté `PRODUCTVISION.md`. |
| 31/07/2026 | **Correction d'architecture importante** (§2, 🅰️) : en lisant `householdService.js` plutôt qu'en supposant, foyers/membres/rôles sont **déjà** sur Supabase — l'affirmation inverse d'une version antérieure de ce document était fausse. Le vrai goulot d'étranglement identifié : le plan 2D/3D est construit et testé mais parle encore à l'ancien backend et n'est même pas monté dans le routing actuel (`/spatial` = placeholder, pas de route `/editor`). Consigne ajoutée en mémoire : mettre à jour ce document et `README.md` proactivement à chaque avancée, sans attendre la demande. |
| 31/07/2026 | Reconnexion du plan 2D/3D à Supabase (chantier 🅰️) : `floorPlanService.js` créé (blob JSONB + verrou optimiste sur `version`), `HouseholdSpatialView.jsx` reconnecté (un seul import changé), monté sur `/spatial` via `HouseholdSpatialPage.jsx`. Pas encore testé en conditions réelles (prochaine étape). |
| 01/08/2026 | **Test réel confirmé par Paul** : créer un plan, sauvegarder, recharger — fonctionne. Chantier 🅰️ mis à jour en conséquence. Correction d'une incohérence interne à `DATAMODEL.md` (repérée en revérifiant les 4 documents avant une nouvelle conversation) : `password_hash` apparaissait dans l'ERD et le dictionnaire de la table `users` alors que la section 6 du même document affirmait déjà son absence (Supabase Auth gère l'auth dans `auth.users`) — retiré des deux premiers endroits. |
| 01/08/2026 | **Correction de `README.md`** (repérée en inspectant le code de `LayoutEditor.jsx`/`HouseholdSpatialView.jsx`/`FloorView3D.jsx`/`Plan2DView.jsx` avant une reprise de travail sur l'éditeur) : `layout-editor/` (tout entier — `LayoutEditor.jsx`, `RoomInspector.jsx`, `RoomNameModal.jsx`, `layoutGeneration.js`, `roomCollision.js`, `layoutStorage.js`, `validateLayout.js`, `roomTypes.js`) était étiqueté "ANCIEN, dormant" alors qu'il est activement importé et rendu par `HouseholdSpatialView.jsx`, lui-même monté sur `/spatial` via `AppRouter.jsx` — confirmé par la chaîne d'imports réelle, pas supposé. Même erreur pour `FloorView3D.jsx`, `Plan2DView.jsx`, `OnboardingScreen.jsx` et `features/household/utils/pathfinding.js`, tous importés par `HouseholdSpatialView.jsx`. README corrigé : arborescence et note de statut mises à jour, liste des fichiers réellement dormants resserrée à `HouseholdRoot.jsx`/`HousingDashboard.jsx`/`CreateHousingScreen.jsx`/`JoinHousingModal.jsx`/`ScanPlanModal.jsx`/anciens formulaires d'auth/`App.jsx`/`api.js`/`householdLayoutApi.js`. ~~`PROJECT.md` §3.2 n'était pas affecté~~ **Correction du jour suivant : si, en partie** (voir entrée suivante — l'affirmation sur l'aimantage dans ce même §3.2 était, elle, obsolète). |
| 01/08/2026 | **Refonte mur-arête** (raffinement de l'éditeur déjà construit, §3.2 — demande explicite de Paul, priorité donnée à la vue 2D avant la 3D ; distinct du chantier 🅲 de la feuille de route, qui concerne les tâches multi-pièces) : remplace le modèle où les murs étaient des **dalles pleines** (1 case de grille consommée par mur, et bug de fusion visuelle quand deux pièces se touchaient directement, faute de case disponible pour un mur entre elles) par un modèle où les murs sont des **arêtes** (segments sur la bordure des cases), calculées à la volée par la nouvelle fonction `computeRoomEdges()` (`layout-editor/utils/layoutGeneration.js`) à partir de `rooms`+`doors`, jamais stockées. Algorithme vérifié par test Node réel (11 assertions, 4 géométries dont Salon/Couloir/Cuisine en L) — pas juste relu, y compris exécuté contre le vrai fichier du projet. `generateFloorTiles()` ne produit plus que des dalles de sol ; nouveau composant partagé `WallEdges.jsx` (SVG) pour le rendu des murs/portes, utilisé par `Plan2DView.jsx` (réécrit) et réutilisable plus tard par `FloorView3D.jsx`. `LayoutEditor.jsx` : outil de porte adapté aux arêtes (candidats = cloisons entre pièces qui se touchent réellement, plus d'auto-détection à 1 case d'écart — **inverse de l'ancien comportement**, à savoir avant de tracer un plan). Propagé à `HouseholdSpatialView.jsx` (`floorEdges`), `floorPlanService.js` (persistance de `orientation`) et `validateLayout.js` (schéma Zod des portes). **Correction au passage** : §3.2 affirmait un aimantage magnétique pendant le glissé — faux, vérifié en relisant `roomCollision.js`/`LayoutEditor.jsx` : l'aimantage a été retiré à une demande antérieure et jamais réintroduit (seule la résolution anti-chevauchement au relâchement existe). **Non fait volontairement** (priorité 2D confirmée par Paul) : `FloorView3D.jsx` et `pathfinding.js` restent sur l'ancien modèle tile-based — régression connue et acceptée temporairement (l'avatar pourra traverser un mur sans porte une fois `tiles` privé de ses dalles "wall"/"door"), à corriger avant de rouvrir ce chantier. `DATAMODEL.md` §5 étoffé (structure du blob + modèle mur-arête, jusque-là un stub). **Vérification non faite** : composants React non testés dans un vrai navigateur/Vite (pas d'accès réseau dans cet environnement) — à valider chez Paul avant de considérer le chantier terminé. Point pratique : tout plan de test déjà enregistré avec des portes doit être réinitialisé (portes au format `{x,y}` sans `orientation`, plus reconnues). |
| 01/08/2026 | **Renommage terminologique et de fichiers** (demande explicite de Paul) : (1) le vocabulaire "vue 2.5D"/"2.5D" devient **"vue 3D"/"3D"** partout — code, commentaires, documentation (changement de VOCABULAIRE uniquement, le rendu de `FloorView3D` reste une grille CSS vue de dessus avec avatar, pas un moteur isométrique/WebGL ; à confirmer avec Paul si un vrai rendu 3D est souhaité un jour, ce serait un chantier distinct). (2) `ApartmentSpatialMvp.jsx`/`.css` → **`HouseholdSpatialView.jsx`/`.css`** (plus un prototype "MVP" depuis longtemps ; "Apartment" retiré, l'app cible des logements en général, pas spécifiquement des appartements) ; `FloorView2D.jsx`/`.css` → **`FloorView3D.jsx`/`.css`**. Préfixe CSS `spatial-mvp__` → `spatial-view__`. Appliqué de façon exhaustive (grep de vérification à zéro résidu) : code source (frontend + backend dormant), toute la documentation (`README.md`, ce document y compris les entrées de journal déjà datées, `DATAMODEL.md`, `PRODUCTVISION.md`, `TODO.md`, `ROUTINGANDUSERFLOWS.md`, `USERFLOWONBOARDING.md`) — même logique que le renommage antérieur de `CHEZ_NOUS_SUIVI_PROJET.md` : le nom courant partout plutôt qu'un mélange ancien/nouveau selon la date de l'entrée. Deux incohérences trouvées et corrigées au passage dans l'en-tête de `HouseholdSpatialView.jsx` (sans lien avec le renommage) : `householdId`/`isOwner`/`role` étaient documentés comme venant de `HouseholdRoot.jsx`/`HouseholdViewPage.jsx` (dormants depuis la bascule Supabase) alors qu'ils viennent de `HouseholdSpatialPage.jsx` (`useParams`/`useOutletContext`) ; et `FloorView3D.jsx` était toujours documenté "100% mock, pas d'appel API" alors qu'il reçoit des données réelles en usage normal. **Non touché, hors sujet** : `HouseholdViewPage.jsx`/`.css`, physiquement toujours présents dans le zip fourni alors que `README.md`/`TODO.md` les documentent comme supprimés — à vérifier côté dépôt réel de Paul (export antérieur à la suppression ?). |
| 01/08/2026 | **Verrou de pièce dans `LayoutEditor.jsx`** (demande explicite de Paul, corrige au passage un bug signalé : le nom de pièce caché derrière la poignée ✢) : nouveau cadenas par pièce (coin supérieur gauche), remplace l'ancien mécanisme de "sélection" par tap rapide (`selectedRoomId` retiré, `unlockedRoomId` le remplace). Verrouillée = nom+superficie affichés, déplacement/redimensionnement désactivés ; déverrouillée = poignée ✢ + poignées de redimensionnement, bordure émeraude. Une seule pièce déverrouillée à la fois, reverrouillage automatique de la précédente au début d'un nouveau tracé et à l'enregistrement ; pièce fraîchement créée démarre déverrouillée. Petite robustesse ajoutée au passage : `stopPropagation` sur `pointerDown` (pas seulement `onClick`) pour le cadenas ET le bouton supprimer — évite qu'un appui long parasite s'arme pendant le clic (même pattern déjà utilisé pour l'ouverture de l'Inspecteur). Non vérifié en navigateur réel (pas d'accès réseau ici) — à confirmer chez Paul. |
| 01/08/2026 | **"Ajouter une porte" → "Retirer un mur"** (demande explicite de Paul : le résultat visuel ne doit pas ressembler à un objet de porte posé, mais à une absence de mur — deux pièces peuvent très bien rester séparées par un mur plein sans aucune ouverture). Renommage conceptuel dans `layoutGeneration.js` : `kind: "door"` → `"opening"` (valeur calculée, jamais stockée — aucun impact sur les données existantes), `findDoorCandidates` → `findOpenableWallSegments`, paramètre `doorEdges` → `openingEdges` (répercuté dans `HouseholdSpatialView.jsx`). `WallEdges.jsx` : une arête "opening" ne dessine plus AUCUN trait (avant : trait pointillé ambré façon porte) — mur réellement retiré, pas stylé différemment. `LayoutEditor.jsx` : outil renommé en interne (`wallToolActive`, `toggleWallSegment`, `findOpenableWallSegments`), bouton "🧱 Retirer un mur" (était "🚪 Ajouter une porte"), style des segments candidats/ouverts revu (neutre gris foncé "cliquable" / clair "ouvert", plus d'ambré "porte posée"). Le champ persisté (`doors` dans le blob Supabase) n'a volontairement PAS été renommé — changement purement conceptuel et d'interaction, aucune migration de données nécessaire, tout plan déjà enregistré profite du changement immédiatement au rendu (la valeur `kind` est dérivée à la volée, jamais stockée). `DATAMODEL.md` §5 et `PROJECT.md` §3.2 mis à jour en conséquence. Non vérifié en navigateur réel. |
| 01/08/2026 | **Trois correctifs/fonctionnalités signalés après test réel par Paul** (le déploiement précédent fonctionne — premier retour de test en conditions réelles sur ce chantier) : (1) **Bug réel trouvé et corrigé** : "retirer un mur" n'avait aucun effet visible dans l'éditeur — `LayoutEditor.jsx` ne rendait jamais de vrai mur (seulement la bordure CSS individuelle de chaque pièce, insensible aux ouvertures), corrigé en y important `WallEdges.jsx` avec des arêtes recalculées en direct (voir §3.3). (2) **Comportement corrigé** : "Réinitialiser le plan" basculait vers l'écran d'accueil ("onboarding") — reste maintenant en Mode Édition sur un plan vierge (voir §3.3, `handleResetLayout`). (3) **Nouvelle fonctionnalité** : "+ Ajouter un étage" / "🗑️ Supprimer un étage" (voir §3.3, `handleStartAddFloor`/`handleDeleteFloor`, nouvelle fonction backend `deleteFloor` dans `floorPlanService.js`) — a révélé et corrigé au passage un bug latent (`handleSaveLayout` donnait toujours "Rez-de-chaussée"/RDC/niveau 0 à un nouvel étage, même si un premier existait déjà). Non vérifié en navigateur réel (pas d'accès réseau ici) — à confirmer chez Paul. |
| 01/08/2026 | **"Réinitialiser le plan ne fait rien" (signalé par Paul juste après le correctif précédent)** : la 1ère correction (rester en Mode Édition au lieu de rebasculer vers l'accueil) ne suffisait pas — le vrai bug était que `LayoutEditor.jsx` initialise son état local (`rooms`/`doors`) via `useState(existingRooms)`, qui ne se ré-exécute JAMAIS quand les props parent changent sans démontage (pas de `key`). Le bouton appelait `onReset` -> `resetHouseholdLayout` (backend, TOUT le foyer) -> props vidées côté parent -> mais l'état local affiché à l'écran ne bougeait pas d'un pixel, quoi que fasse le backend. **Précision de Paul au passage** : "Réinitialiser" doit de toute façon être scopé à UN étage, pas à tout le foyer (cohérent avec l'ajout de "+ Étage"/"🗑️ Supprimer un étage" du correctif précédent). Solution : `handleConfirmReset` vide maintenant l'état local de `LayoutEditor.jsx` directement (pièces, ouvertures, tout l'état d'interaction), sans prop `onReset` ni appel backend — même logique que "Annuler" (rien n'est persisté avant "Enregistrer le plan" explicite). Prop `onReset` retirée de `LayoutEditor.jsx` ; `handleResetLayout` (devenu mort, plus aucun appelant) retiré de `HouseholdSpatialView.jsx`. Bouton renommé "🗑️ Réinitialiser cet étage" (portée). Non vérifié en navigateur réel. |
| 02/08/2026 | Intégration de 4 prototypes (Profil, Réglages, Préférences, Calendrier — v0.1.2) : nouveau dossier `features/account/` (`ProfilePage.jsx`, `SettingsPage.jsx`, `PreferencesPage.jsx` + CSS partagé), `HouseholdCalendarPage.jsx` remplacé par une vraie grille mensuelle dynamique, routes `/profile`/`/settings`/`/settings/preferences` ajoutées, `UserMenu.jsx` n'a plus "Profil"/"Réglages" désactivés. Voir §3.10 pour le détail complet et les écarts assumés (stats/badges Profil à zéro plutôt que les chiffres de démo du prototype, bouton retour ajouté sur Profil). **Correction au passage** (§2) : le schéma d'architecture décrivait encore le plan 2D/3D comme "construit mais sur l'ancien backend, pas monté dans le routing" — obsolète depuis sa reconnexion Supabase et son test réel par Paul (déjà noté en §3.9 et dans ce journal, mais jamais répercuté dans le schéma lui-même). Corrigé, et "Calendrier" retiré du bucket "aucune donnée ni UI" pour rejoindre un nouveau palier intermédiaire ("UI construite, pas encore persistée") aux côtés de Profil/Réglages/Préférences. |
| 02/08/2026 | **`ProfilePage.jsx` refaite** : le prototype v0.1.2 utilisé initialement était erroné (fourni par erreur) — Paul a transmis le v0.1.1, la vraie référence. Différences absorbées : bouton retour (confirmé, déjà ajouté par déduction sur le v0.1.2) ; bouton Réglages du header retiré (absent de ce prototype, uniquement "Partager") ; avatar "Sims" (plumbob, humeur, "Studio Dressing 3D") remplacé par un cadre à bordure dégradée + initiales en repli + bouton caméra (aperçu local uniquement, même principe que l'icône de foyer). Deux nouveaux écarts assumés : coche "Membre vérifié" retirée (aucun système de vérification n'existe) ; badge "Membre ChezNous Plus" simplifié en "Membre" (aucun système d'abonnement réel, afficher "Plus" laisserait croire à un palier payant actif) ; "Membre de [foyer] depuis" adapté en "Membre depuis [date de création du compte, réelle]" (cette page n'est pas imbriquée dans un foyer précis). |
| 02/08/2026 | **Audit complet des effets de conversion prototype → React** (nouvelle directive enregistrée en mémoire : ne jamais supprimer/simplifier halos, `backdrop-filter`, `.card-glow`). En vérifiant honnêtement plutôt que de simplement confirmer la directive, trouvé une régression plus large que prévu — présente depuis `HouseholdHomePage.jsx` (la toute première page traduite) jusqu'aux pages les plus récentes : (1) `.card-glow` (bordure à dégradé) n'avait **jamais** été implémenté nulle part, malgré son usage dans les prototypes Accueil et Calendrier ; (2) plusieurs boutons "verre" (`.glass` dans les prototypes) n'avaient qu'un fond `rgba()` plat sans `backdrop-filter` : icône de foyer (`HouseholdSwitcher.jsx`), déclencheur du menu profil (`UserMenu.jsx`), modale streak (`PlaceholderModal.jsx`), boutons retour/partager des pages compte ; (3) les halos lumineux d'ambiance (coin haut-gauche émeraude, coin bas-droit lime), présents en arrière-plan de quasiment tous les prototypes, n'existaient sur AUCUNE page. Corrigé en 3 temps : `.card-glow` et `.ambient-glow*` ajoutés comme utilitaires globaux dans `theme.css` (réutilisables plutôt que réinventés par page) ; nouveau composant partagé `components/ui/AmbientGlow.jsx` ; halos appliqués **une seule fois** au niveau de la coquille `AppLayout.jsx` (couvre les 6 onglets du foyer d'un coup) + une fois par page compte (Profil/Réglages/Préférences, qui ne bénéficient pas d'`AppLayout`). `backdrop-filter` ajouté partout où il manquait ; `card-glow` ajouté sur la carte "Vue du foyer" (Accueil) et la grille du mois (Calendrier). Vérifié par recherche exhaustive dans tous les prototypes (`login/`, `new/`, `signup/` n'utilisent ni `.glass` ni `.card-glow` — hors périmètre, rien à corriger là). |
| 02/08/2026 | **Réécriture complète de `theme.css` et `Icons.jsx`** (demande explicite de Paul, fichiers fournis directement plutôt qu'un zip). `theme.css` : configuration Inter (graisses 400-900) ajoutée ; `.glass`/`.card-glow`/`.no-scrollbar`/`.ambient-glow*` consolidés en utilitaires globaux (reprennent et complètent l'audit de l'entrée précédente) ; animation `@keyframes plumbob-float` ajoutée mais **non utilisée actuellement** (spécifique à l'avatar "Sims" du prototype Profil v0.1.2, qui s'est révélé erroné — conservée en cas de reprise future du concept). **Changement de direction visuelle majeur pour `Icons.jsx`** : les 51 icônes passent d'un style "traits fins" (contour seul, sans remplissage) à un style "solide/plein" (`fill: currentColor`, détails internes via `fillRule="evenodd"`) — reproduit l'esprit du style `fa-solid` des prototypes sans recopier leurs tracés exacts (bibliothèque commerciale). Tous les noms de fonctions et props (`size`/`className`) inchangés — vérifié par diff qu'aucun des 51 exports n'a changé de nom, aucun fichier appelant ne casse. Les deux fichiers compilés avec `esbuild` (déjà présent dans l'environnement) pour confirmer l'absence d'erreur de syntaxe réelle, pas seulement un comptage de balises. Mémoire mise à jour en conséquence ; au passage, deux blocs de mémoire "vision" quasi dupliqués fusionnés en un seul, et une référence obsolète "2D/2.5D" (renommée "2D/3D" depuis plusieurs sessions, jamais répercutée dans la mémoire jusqu'ici) corrigée. **Non vérifié** : rendu réel des icônes dans un navigateur (pas d'accès réseau ici) — en particulier les icônes utilisant `fillRule="evenodd"` avec plusieurs sous-tracés, à valider visuellement chez Paul. |
| 02/08/2026 | **Reprise du chantier éditeur** (vérification complète du zip re-fourni au préalable — tout intact, un seul fichier `HouseholdSpatialView.jsx` périmé et orphelin trouvé dans `layout-editor/components/`, sans importeur, laissé à la charge de Paul) — **deux ajustements signalés par Paul** : (1) **"+ Ajouter un étage" déplacé** de l'écran de consultation du plan vers `LayoutEditor.jsx` — c'était une action D'ÉDITION mal placée dans l'écran de LECTURE. Nouveau prop `onAddFloor` ; le bouton vide d'abord le brouillon local de l'éditeur (réutilise `handleConfirmReset`), avec confirmation seulement s'il y a un brouillon non vide à perdre, puis prévient `HouseholdSpatialView.jsx` via `onAddFloor` (= `handleStartAddFloor`, inchangé). "🗑️ Supprimer un étage" **reste** dans l'écran de consultation (nature différente : supprime un étage déjà enregistré côté serveur, pas un brouillon). (2) **Retour après enregistrement** : bascule maintenant sur le Plan 2D (`viewMode="2d"`) plutôt que la Vue 3D — **remplace** une préférence antérieure de Paul pour la 3D après sauvegarde. Non vérifié en navigateur réel. |
| 02/08/2026 | **Quadrillage inversé + rectangle de pièce (`Plan2DView.jsx`)** — deux correctifs signalés par Paul avec capture d'écran à l'appui : (1) le quadrillage était visible sur le PLAN (une bordure fine par dalle, `.plan2d-tile`) mais quasi invisible dans l'ÉDITEUR (`rgba(0,0,0,0.12)`, trop discret) — c'est l'inverse de ce qui est utile (le quadrillage aide à tracer, pas à consulter). Opacité du quadrillage de `LayoutEditor.css` doublée (`0.22`) ; `Plan2DView.jsx` réécrit pour ne plus dessiner de dalles du tout. (2) **Une pièce = un seul rectangle de couleur**, avec nom + nombre de tâches actives affichés dessus (exemple visuel fourni par Paul) — dérivé directement du rectangle `{x,y,width,height}` de la pièce, `tiles` n'est plus utilisé par ce composant. **Tâches actives = placeholder honnête** : `room.activeTaskCount` n'existe dans aucune donnée réelle (le chantier Tâches n'a pas commencé), retombe systématiquement sur "Aucune tâche" — le composant est prêt à afficher un vrai nombre dès qu'une requête existera (voir `TODO.md`). Murs/ouvertures : gardés sur `WallEdges` (pas un contour uniforme par pièce, qui ignorerait les ouvertures et referait fusionner visuellement deux pièces séparées par une cloison pleine avec le vide — l'inverse du bug déjà corrigé sur ce sujet). Non vérifié en navigateur réel. |
| 02/08/2026 | **Trois demandes de Paul avec capture d'écran à l'appui** (même capture que le correctif précédent, message suivant) : (1) **"🗑️ Supprimer cet étage" déplacé dans l'éditeur** — même logique que "+ Ajouter un étage" plus tôt, retiré du menu de vue (bouton + modale de confirmation), ajouté dans `LayoutEditor.jsx` (masqué si l'étage n'existe pas encore côté serveur). (2) **Type ET couleur choisis dès la création d'une pièce** : `RoomNameModal.jsx` renommé `RoomCreateModal.jsx`, demande maintenant type (grille d'icônes) + couleur (nouvelle grille de 14 couleurs, `ROOM_COLORS` dans `roomTypes.js`) en plus du nom, en une seule étape. (3) **Couleur découplée du type** : avant, choisir un type écrasait TOUJOURS la couleur (dérivée du type) — `handleUpdateRoomType` ne touche plus qu'au nom/icône, nouvelle fonction `handleUpdateRoomColor` séparée, `RoomInspector.jsx` porte la même grille de couleurs que la création pour changer une pièce déjà créée. Vérifié par compilation réelle (`esbuild`, disponible via `tsx` dans cet environnement) sur les 4 fichiers touchés — zéro erreur de syntaxe, pas seulement un comptage de balises. Non vérifié en navigateur réel (rendu visuel des nouvelles grilles). |
| 02/08/2026 | **Deux nouvelles captures d'écran, deux corrections + une clarification** : (1) **Bouton renommé** — "🗑️ Réinitialiser cet étage" → **"🗑️ Réinitialiser le plan"** (demande explicite de Paul : redondant avec "Supprimer cet étage", voisin dans la même barre d'outils depuis le correctif précédent). (2) **Clarification, pas un changement** : Paul demandait aussi d'"ajouter un bouton 'ajouter un étage'" — déjà présent (ajouté deux corrections plus tôt), visible sur sa propre capture d'écran ; signalé sans le dupliquer. (3) **Plan2DView : ajustement automatique à l'écran + zoom**, voir §3.3 pour le détail — `cellPx` fixe remplacé par un calcul dérivé de la taille réelle du conteneur (`ResizeObserver`), tout l'appartement tient par défaut ; boutons +/- ("Tout voir" pour réinitialiser) pour qui veut zoomer, sans bibliothèque tierce (cohérent avec la décision déjà prise d'éviter `react-zoom-pan-pinch`). Vérifié par compilation réelle (`esbuild`) sur les 2 fichiers touchés. Non vérifié en navigateur réel. |
| 03/08/2026 | **Recadrage dynamique sur bounding box (`Plan2DView.jsx`)** — nouvelle capture d'écran de Paul montrant un grand bloc gris vide à droite des pièces, malgré le correctif "ajustement automatique" de la veille. Diagnostic de Paul (précis, confirmé) : le calcul divisait par `floor.gridWidth`/`gridHeight` (la grille COMPLÈTE de l'étage, souvent bien plus large que les pièces tracées), pas par leur emprise réelle. Corrigé selon la spec fournie par Paul : `minX`/`minY`/`maxX`/`maxY` des pièces recalculés à chaque rendu, +1 case de marge de chaque côté (`BBOX_PADDING_CELLS`), pièces et arêtes (`WallEdges`, nouveau paramètre optionnel `offsetXPx`/`offsetYPx`, défaut 0 pour ne rien changer à `LayoutEditor.jsx`) décalées en conséquence. `.plan2d-scroll` centre son contenu (flex, `flex-shrink:0` sur `.plan2d-grid` pour ne pas l'écraser si un zoom manuel dépasse le cadre). Logique vérifiée par simulation (Node, 18 assertions, géométrie à 4 pièces proche de la capture) avant d'être écrite — pas seulement une compilation. Compilation réelle (`esbuild`) confirmée sur les 2 fichiers touchés. Non vérifié en navigateur réel. |
| 03/08/2026 | **Zones grises persistantes en dézoomant (`Plan2DView.jsx`)** — troisième capture d'écran de Paul sur ce sujet : dézoomer EN DESSOUS de "Tout voir" révélait du vide, puisque le cadre extérieur (`.plan2d-scroll`) garde toujours sa taille maximale fixe — seul le contenu rétrécissait dedans. `ZOOM_MIN` passé de `0.5` à `1` : "Tout voir" (l'ajustement automatique) EST déjà le dézoom minimum utile par construction (`baseCellPx` choisit déjà la plus grande cellule qui tient entièrement dans le cadre) — aller plus bas n'ajoute que du vide, jamais d'information. Boutons +/- désactivés visuellement (`disabled`, opacité réduite) aux deux bornes pour que ce soit clair. Vérifié par compilation réelle (`esbuild`). Non vérifié en navigateur réel. |
| 03/08/2026 | **Bandes grises restantes même à "Tout voir" (`Plan2DView.css`)** — quatrième capture d'écran de Paul : même au dézoom minimum désormais correct, des bandes grises restaient en haut/bas du "carré noir". Cause : `.plan2d-scroll` (fond + coins arrondis) garde une taille FIXE (480px/65vh) indépendante du contenu — dès que le ratio largeur/hauteur de l'appartement ne correspond pas exactement à celui de ce cadre, une dimension (ici la hauteur) reste sous-utilisée. Un cadre extérieur de taille fixe ne peut PAS coller exactement à un contenu de ratio variable sans déformer les cellules (les rendre non carrées) — ce n'était pas souhaitable. Solution : le fond/les coins arrondis déplacés de `.plan2d-scroll` vers `.plan2d-grid` (qui, lui, colle déjà exactement à la taille réelle du plan + le padding d'1 case) — `.plan2d-scroll` devient un cadre de MESURE invisible (transparent), plus un "carré noir" visible séparément. Changement CSS uniquement, aucune modification JS. Non vérifié en navigateur réel. |
| 03/08/2026 | **Zoom manuel entièrement retiré (`Plan2DView.jsx`/`.css`)** — après trois allers-retours pour corriger le comportement du zoom (bornes, bandes grises), Paul a demandé de retirer complètement l'option. État `zoom`, constantes `ZOOM_MIN`/`ZOOM_MAX`/`ZOOM_STEP`, les 3 boutons et tout le CSS associé supprimés — `cellPx` est directement l'ajustement automatique (anciennement `baseCellPx`), plus de multiplicateur. La vue affiche désormais TOUJOURS "tout voir", sans contrôle. Commentaires mis à jour en conséquence (plusieurs faisaient encore référence au zoom manuel dans `Plan2DView.css`). `overflow: auto` conservé comme filet de sécurité (grand appartement où `MIN_CELL_PX` empêcherait de tout faire tenir). Vérifié par compilation réelle (`esbuild`). Non vérifié en navigateur réel. |
| 03/08/2026 | **Vue du foyer (Accueil) : plan réel intégré (`HouseholdHomePage.jsx`)** — demande explicite de Paul, capture d'écran annotée à l'appui (deux captures, celle de l'Accueil et celle du Plan, comparées côte à côte). La carte "Pièces du logement" affichait des pastilles statiques imaginaires (Cuisine/Salon/SDB) — remplacées par le VRAI `Plan2DView.jsx` (même composant que l'onglet Plan), chargé via `fetchHouseholdLayout` (même service que `HouseholdSpatialView.jsx`), affichant `floors[0]` (même convention de sélection, pas de tri par `level` — limite préexistante, pas introduite ici). Deux props ajoutés à `Plan2DView.jsx` pour permettre cette réutilisation compacte : `maxHeight` (200px ici, au lieu de 65vh pensé pour plein écran) et `showHint` (masqué, la carte a déjà sa propre indication "Toucher pour voir le plan en détail"). Pièces non cliquables individuellement dans cet aperçu (`onSelectRoom` non passé) : la carte entière navigue déjà vers l'onglet Plan au moindre tap, une navigation par pièce en plus aurait fait doublon. Au passage, couleur du texte d'aide de `Plan2DView.jsx` basculée sur `var(--text-muted)` (cohérence avec le thème sombre des pages récentes — était encore un gris pensé pour fond clair). "Propreté globale" (78%, mock) volontairement conservée telle quelle : non demandée, et aucune donnée réelle n'existe pour la calculer de toute façon (chantier Tâches pas commencé). Vérifié par compilation réelle (`esbuild`) sur les 2 fichiers `.jsx` touchés. Non vérifié en navigateur réel. |
| 03/08/2026 | **Vue détaillée de pièce + filtre propreté** (demande explicite de Paul, 2 prototypes HTML fournis : `ui_2d_v0.3.1.html`, `ui_room_v0.2.1.html`) — voir §3.3 pour le détail complet. Résumé : clic sur une pièce → `RoomDetailView.jsx` (nouveau) au lieu de la Vue 3D, avec retour vers le Plan 2D ; murs blancs 2px (`WallEdges.jsx`, nouveau paramètre `strokeColor`, PAS un contour par pièce comme le prototype pour ne pas réintroduire le bug d'ouverture déjà corrigé) ; filtre propreté/heatmap (0=vert/1=jaune/2=orange/3+=rouge, 2 tokens ajoutés à `theme.css`) ; `Plan2DView.css` enfin basculé sur les variables de `theme.css` (demande explicite, gardait un thème clair codé en dur depuis le début malgré la bascule du reste de l'app vers le sombre). Icônes : `components/ui/Icons.jsx` pour toute la nouvelle interface, système d'icônes emoji des TYPES de pièce laissé inchangé (`roomTypes.js`, hors périmètre de la demande). Vérifié par compilation réelle (`esbuild`) sur les 5 fichiers `.jsx` touchés/créés. Non vérifié en navigateur réel. |
| 03/08/2026 | **Confirmation du workflow de Paul + décision produit sur la Vue 3D.** Paul confirme son cycle de travail : intègre systématiquement les fichiers livrés, teste en vrai navigateur, ne pousse sur git que si ça fonctionne — "actuellement tout fonctionne comme prévu (pas parfait, pas très beau, mais suffisant)". Autrement dit : tout le travail Plan 2D/`RoomDetailView`/verrou/couleurs livré jusqu'ici a bien été testé en conditions réelles par Paul, malgré les mentions répétées de ce document "non vérifié en navigateur réel" (qui signalaient l'absence de vérification *de mon côté*, dans cet environnement sans accès réseau — pas une absence de vérification tout court). **Décision produit** : Vue 3D mise complètement de côté ("elle viendra à la toute fin, je ne pense vraiment pas qu'elle soit importante") — voir §5/🅰️ pour la note technique correspondante, `TODO.md` #4 repriorisé. **Prochain chantier confirmé** : 🅲 (Tâches, multi-pièces) — recommandé par Claude (le Plan est la base qui manquait, il est maintenant assez avancé pour construire dessus ; la Vue 3D est un problème contenu, pas structurant), accepté par Paul. Périmètre initial proposé, pas encore commencé : une tâche = une seule pièce pour commencer (`tasks.room_id` existant, pas encore `task_rooms`), câblée d'abord dans `RoomDetailView.jsx` qui attend déjà ces données (placeholder actuel, voir `TODO.md` #11). |
| 03/08/2026 | **Reprise du chantier 🅲 dans un nouveau fil** (documents de référence déplacés à la racine sans underscore entre-temps, voir en-tête — `docs/PROJET.md`/`docs/DATA_MODEL.md` devenus `PROJECT.md`/`DATAMODEL.md` à la racine). Avant tout code, inspection du zip fourni : schéma réel de `tasks` relu directement dans `01_schema_and_rls.sql`/`02_reconcile_with_data_model.sql` (pas seulement `DATAMODEL.md`), confirmation qu'aucun prototype `docs/ui/tasks/` n'existe encore, confirmation qu'aucun composant frontend actif ne consomme les colonnes réelles de `tasks` (`HouseholdTasksPage.jsx`/`RoomDetailView.jsx` = placeholders/mock uniquement) — donc aucun risque à retravailler le schéma librement. **Migration proposée puis validée** : `backend/supabase/migrations/04_tasks_v1_schema.sql` — `tasks` enrichie (`importance`, `deadline_offset_days`, `task_group_id`, `depends_on_task_id`, `depends_on_every_n`), nouvelle table `task_groups` (remplace `task_rooms`), nouvelle table `task_assignees` (remplace `assigned_to`/`assigned_to_occupant_id`, migration des données existantes puis suppression des deux colonnes). **Exécutée avec succès par Paul sur le projet Supabase réel** (capture d'écran du SQL Editor, script complet sans erreur). Deux décisions de schéma actées par défaut faute d'objection (Paul a exécuté le fichier tel quel) : suppression d'un groupe → `ON DELETE SET NULL` plutôt que cascade ; pas de `CHECK` dur en base sur `due_date`/`deadline_offset_days`, règle laissée au formulaire frontend. Voir §3.11 pour le détail complet, `DATAMODEL.md` §2/§7 pour le schéma documenté. **Non fait** : aucun service ni UI frontend ne branche encore ce schéma — prochaine étape de ce chantier. |
| 03/08/2026 | **Deux nouvelles idées produit de Paul, documentées et pas codées** (`PRODUCTVISION.md` §9, nouveau, deux volets 9.1/9.2 ; chantier 🅺 ajouté en Phase 3 ci-dessus) : Vue "Sécurité" en toggle sur le plan (statut ouvert/fermé des portes/fenêtres, V1 déclaratif puis capteurs IoT réels) et électroménager connecté (déclenchement/notification de tâches depuis de vrais appareils). Explicitement hors périmètre de la Phase 1/2 en cours (moteur de tâches), confirmé par Paul. **Point technique important soulevé côté Claude avant tout code futur** : le modèle actuel d'ouverture (`doors`, `DATAMODEL.md` §5) représente un mur retiré de façon permanente, pas un objet porte/fenêtre avec un état qui varie dans le temps — donnera lieu à une vraie décision de modèle (champ `type`, fenêtres à concevoir de zéro) le moment venu, pas un simple ajout de colonne `isOpen`/`sensorId` comme envisagé initialement par Paul. Recommandation d'intégration IoT unique (Home Assistant) plutôt que des connecteurs par marque, pour les deux idées — voir §9 pour le détail des marques évaluées (Home Connect/Tuya/SmartThings plus réalistes qu'iRobot, dont l'API grand public est largement fermée). |
| 03/08/2026 | **Branchement frontend des Tâches (lecture + bascule de statut), §3.12** — décision prise avec Paul : service d'abord, écran de création riche reporté à une session dédiée. `taskService.js` (nouveau) + `useHouseholdTasks.js`/`useHouseholdRoomNames.js` (nouveaux hooks) branchés sur `HouseholdSpatialView.jsx` (compteur réel par pièce → `Plan2DView`, ferme `TODO.md` #10), `RoomDetailView.jsx` (liste réelle, case à cocher persistée, ajout rapide — ferme `TODO.md` #11 largement) et `HouseholdTasksPage.jsx`/`.css` (nouveau, remplace `TabPlaceholder` par une vraie liste groupée par pièce, périmètre volontairement minimal, aucun prototype ne couvrant cet écran). **Deux éléments du prototype `RoomDetailView` retirés plutôt que fabriqués** (gemmes/gains potentiels — gamification pas commencée ; "Dernier ménage" précis — pas de colonne `completed_at`/`completed_by`), remplacés par deux compteurs 100% réels. **Deux découvertes ajoutées à `TODO.md` (#12, #13) plutôt que traitées par anticipation** : absence de `completed_at`/`completed_by` sur `tasks` ; et une régression trouvée en marge (`.card-glow`/`.ambient-glow*`/`.no-scrollbar` référencées par le code et documentées comme ajoutées à `theme.css` le 02/08/2026, mais absentes du `theme.css` réel de ce zip — halos d'ambiance actuellement invisibles partout dans l'app, non corrigé ici pour éviter de réinventer ces styles de mémoire). Fichiers JS purs vérifiés par `node --check` ; JSX relus manuellement, **aucun accès npm/esbuild dans cet environnement cette fois** (réseau désactivé, `node_modules` jamais installé). Non vérifié en navigateur réel. |
| 03/08/2026 | **Extraction de `features/tasks/` (§3.6)** — Paul remarque à raison que `features/household/` atteint 58 fichiers (+ `utils/`) et demande si un nouveau dossier dédié serait plus cohérent, en référence à l'extraction déjà faite de `layout-editor/`. **Réponse : oui**, même logique appliquée : `taskService.js`, `useHouseholdTasks.js`, `useHouseholdRoomNames.js`, `HouseholdTasksPage.jsx`/`.css` déplacés vers `features/tasks/` (nouveau). `RoomDetailView.jsx` délibérément **laissé dans `household/`** — c'est une vue de pièce qui affiche des tâches, pas un fichier du domaine tâches, même distinction que `HouseholdSpatialView.jsx` (resté) vs `LayoutEditor.jsx` (parti) lors de l'extraction précédente. 3 imports corrigés (`HouseholdSpatialView.jsx`, `AppRouter.jsx`, et `useHouseholdRoomNames.js` → `../household/floorPlanService`) ; `useHouseholdRoomNames.js`/`useHouseholdTasks.js`/`taskService.js` revérifiés par `node --check` après déplacement (chemins relatifs vers `lib/supabaseClient.js` inchangés — même profondeur `../../` qu'avant). `household/` passe de 58 à 54 fichiers. |
| 03/08/2026 | **Paul reformule la question, cette fois pour tout `frontend/src`** : "d'autres fichiers/dossiers à réorganiser ? d'autres features à regrouper ?" — zip re-fourni "au cas où". **Bonne pratique appliquée avant de répondre** : re-diffé le nouveau zip contre la copie de travail plutôt que de supposer qu'il était identique — confirmé que le code frontend correspondait exactement à ce qui avait été livré (Paul avait bien intégré tel quel), avec deux vrais écarts : `docs/ui/tasks/ui_task_v0.1.1.html.html` (nouveau, prototype de l'écran de création — mis de côté pour cette session-là) et 3 `.md` qui ne différaient qu'en fin de ligne (CRLF/LF, aucun contenu changé). Audit complet mené en traçant les VRAIES chaînes d'import depuis les deux seules racines réelles (`AppRouter.jsx`, `main.jsx`), pas par supposition. Deux recommandations faites : extraire `features/floor-plan/` (le plus gros morceau de `household/`, ~10 fichiers) et déplacer `UserMenu.jsx` vers `account/` ; et surtout, supprimer un sous-graphe mort d'une trentaine de fichiers confirmé sans aucun chemin actif. Paul valide les deux + la suppression dans le message suivant. |
| 03/08/2026 | **Exécution complète (§3.6)** : `features/floor-plan/` créé (`HouseholdSpatialView.jsx/.css`, `HouseholdSpatialPage.jsx`, `Plan2DView.jsx/.css`, `FloorView3D.jsx/.css`, `RoomDetailView.jsx/.css` — donc `RoomDetailView.jsx` a finalement bougé, mais vers `floor-plan/` et pas vers `tasks/`, cf. entrée précédente —, `OnboardingScreen.jsx/.css`, `floorPlanService.js`, `mockData.js`, `utils/pathfinding.js`). `UserMenu.jsx/.css` → `features/account/`. **~30 fichiers/dossiers confirmés morts supprimés** (détail complet dans `README.md`) : le reste du cluster `HouseholdRoot`/`HousingDashboard`/`CreateHousingScreen`/`JoinHousingModal`/`ScanPlanModal`/`HouseholdViewPage`/`Dashboard.jsx`, les 5 formulaires d'auth dormants, `components/forms|layout|floorplan|tasks` en entier, 8 des 10 fichiers de `components/ui/` (seuls `Icons.jsx`/`AmbientGlow.jsx` avaient un vrai importeur — contredit une affirmation de longue date de ce document), `hooks/`, `utils/` racine, `api.js`, 5 fichiers `assets/*.css`, et 3 tests frontend qui ne couvraient que ce code mort. **Deux fichiers "déjà supprimés" trouvés encore présents pour de vrai cette fois** (même problème récurrent que `HouseholdViewPage.jsx` avait déjà révélé une fois) : `HouseholdViewPage.jsx/.css` (documenté supprimé à deux reprises sans jamais l'être) et `features/household/Icons.jsx` (copie orpheline, également déjà "documentée supprimée"). 5 imports corrigés au total (`AppRouter.jsx`, `HouseholdHomePage.jsx` ×2, `AppLayout.jsx`, `useHouseholdRoomNames.js`) + `main.jsx` allégé à un seul import CSS global (`theme.css`). Zéro changement d'import interne nécessaire dans `floor-plan/` (déplacement en bloc, imports déjà à la bonne profondeur relative). Fichiers JS purs revérifiés par `node --check`. `household/` passe de 54 à 25 fichiers. |
| 03/08/2026 | **Refonte complète de l'éditeur de plan (§3.13)** — Paul fournit `ui_plan_editor_v0.3.0.html` (jugeait l'éditeur actuel "peu gracieux et peu fonctionnel") avec des consignes strictes d'architecture. Avant de coder : lecture intégrale de `LayoutEditor.jsx` (838 lignes) + 5 fichiers compagnons, pour ne rien régresser d'une mécanique réelle non visible sur un prototype statique. Deux points tranchés avec Paul avant d'écrire le composant : (1) le porte/fenêtre/passage doit **fonctionner réellement** (fenêtre conserve le mur, contrairement à porte/passage) — pas juste un habillage, changement de modèle de données confirmé, pas reporté ; (2) le cadenas par pièce est **retiré**, remplacé par un bouton "Ajuster" global (déverrouille toutes les pièces ET séparations en même temps, reverrouille tout à la sortie) — remplace ma proposition initiale de simple sélection sans mode global. `LayoutEditor.jsx`/`.css` → `PlanEditorView.jsx`/`.css` (renommage complet, y compris dans les commentaires des fichiers voisins). 13 nouvelles icônes (`Icons.jsx`). Changement d'étage réel sans quitter l'éditeur (nouveau `onSwitchFloor`, nécessite un `key` sur le composant pour forcer un remontage). Undo/redo réel (n'existait pas). **Bug systémique trouvé en vérifiant exhaustivement plutôt qu'en supposant que l'extension du modèle suffisait** : le nouveau champ `type` était tronqué à 8 endroits différents du projet (reconstruction champ par champ de l'objet porte) — tous corrigés après un grep complet, pas une relecture partielle. `DATAMODEL.md` §5 mis à jour (modèle porte/fenêtre/passage documenté comme réellement implémenté, plus une anticipation). Vérification : fichiers JS purs par `node --check`, `PlanEditorView.jsx` (~700 lignes) relu manuellement en entier, script Python maison vérifiant tous les imports relatifs et toutes les icônes importées du projet — zéro cassé. Icônes et rendu **non vérifiés visuellement** (pas d'accès réseau ici) — à confirmer chez Paul. |
| 03/08/2026 | **Désync découvert et corrigé** — Paul relance l'app après le "gros transfert" (floor-plan/tasks/suppression du code mort/`PlanEditorView.jsx`) : erreur Vite `Failed to resolve import "../account/UserMenu"`. Diagnostic par comparaison directe du zip fraîchement fourni contre la copie de travail plutôt que par supposition : **seule l'extraction `features/tasks/` avait été réellement appliquée** — `features/floor-plan/` n'existait pas, `UserMenu.jsx` était resté dans `household/`, le code mort (~30 fichiers) était toujours là, `LayoutEditor.jsx` n'avait jamais été remplacé par `PlanEditorView.jsx`. Cause probable : les fichiers "à déplacer" (contenu inchangé) n'avaient pas été fournis en téléchargement — seulement listés dans l'arborescence — sur l'hypothèse qu'un déplacement manuel suffirait ; dans la pratique, pas fiable. **Changement de méthode pour ce genre de resynchronisation à grande échelle** : livraison d'un zip complet du dossier de travail entier plutôt qu'une arborescence DÉPLACER/SUPPRIMER à appliquer à la main. Question annexe traitée à cette occasion : `node_modules` à garder installé localement (jamais committé/transféré) — le réinstaller demande une connexion réseau, ce qui conditionne directement la possibilité de travailler hors-ligne ; l'app elle-même reste **non testable hors-ligne** aujourd'hui (tout passe par Supabase, le backend Node.js dormant ne servirait à rien même relancé). |
| 03/08/2026 | **`CLAUDE.md` réellement appliqué** — Paul signale que le zip envoyé contenait encore `CLAUDEINSTRUCTIONS.md`/`CLAUDEMEMORY.md` plutôt que `CLAUDE.md` (recommandé la session précédente mais jamais copié dans la copie de travail — créé à part, jamais intégré). Corrigé : `CLAUDE.md` ajouté à la racine, les deux anciens fichiers supprimés (suit la recommandation déjà actée : `CLAUDEMEMORY.md` était redondant avec les 5 docs de référence et déjà obsolète). **Vérification systématique complète refaite avant tout nouvel envoi** (15 points : structure de chaque dossier `features/*`, décompte des icônes, absence du code mort, imports/icônes vérifiés par script, tests/migrations/docs) plutôt qu'une simple relecture — une référence à `CLAUDEINSTRUCTIONS.md`/`CLAUDEMEMORY.md` par leur nom trouvée et corrigée dans `README.md` au passage. |










