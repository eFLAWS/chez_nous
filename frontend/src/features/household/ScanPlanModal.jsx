// src/features/household/ScanPlanModal.jsx
// Placeholder pour une fonctionnalité à venir (voir la conversation) :
// scanner un plan dessiné à main levée (photo/import), pour le convertir
// automatiquement en pièces dans l'éditeur. Rien de fonctionnel pour
// l'instant — juste une explication, comme JoinHousingModal.jsx, avec
// lequel ce composant partage le même habillage visuel
// (PlaceholderModal).
import PlaceholderModal from "./PlaceholderModal";

export default function ScanPlanModal({ onClose }) {
  return (
    <PlaceholderModal
      title="Scanner un plan"
      hint="Fonctionnalité à venir — prenez en photo un plan dessiné à la main pour le convertir automatiquement en pièces (pas encore opérationnel)."
      onClose={onClose}
    />
  );
}
